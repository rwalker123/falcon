/target
**/target/
# Scratch directory for `export_map` command output (map JSON dumps).
/exports/
# Cross-compiled Windows playtest packages (scripts/build_windows.sh output).
/dist/
**/*.rs.bk
.DS_Store
/.idea
/.vscode
/cargo.lock
**/.godot
shadow_scale_flatbuffers/src/generated/snapshot_generated.rs
# Nested git worktrees created by the Claude Code harness — keep searches,
# status, and staging in one checkout from descending into the others.
.claude/worktrees/

# Python bytecode from the scripts/texture image toolchain
__pycache__/
*.pyc
