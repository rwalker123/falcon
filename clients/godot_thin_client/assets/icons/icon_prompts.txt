# Bundled Icon Generation Prompts
#
# The icon twin of assets/terrain/texture_prompts.txt. These prompts generate the
# bundled PNG art behind the six icon families:
#
#   assets/icons/fauna/    herd map markers      (ui/FaunaSprites.gd)
#   assets/icons/sites/    food-site markers     (ui/SiteSprites.gd)
#   assets/icons/wonders/  Wondrous Site markers (ui/WonderSprites.gd)
#   assets/icons/stages/   settlement tokens     (ui/StageSprites.gd)
#   assets/icons/crops/    crop-ROLE row marks   (ui/CropRoleSprites.gd)
#   assets/icons/flora/    per-SPECIES row marks (ui/FloraSprites.gd)
#
# The first four share ONE pipeline and ONE house style, so both are documented once
# here rather than per family. THE LAST TWO DO NOT — `crops/` and `flora/` are not map
# markers, they are inline marks inside a text ROW, and they override several of the
# clauses below. Read their own sections before generating one; do not start from the
# house style. Those two share a row, a size and a ground, so `flora/` takes the
# `crops/` sub-style wholesale and its section documents only what differs.
#
# THE LAST TWO ALSO COMPETE FOR ONE SLOT, which is the thing to know before adding art
# to either: a basket row draws ONE mark, and a species icon REPLACES its plant's role
# mark where the art exists (`DetailFormat.flora_composition_lines`). So the two sets
# must not draw the same subjects — see the `flora/` section's own note.
#
# NOTE: prompts predating this file were never committed — only the shipped PNGs
# survive. The house style below is reconstructed from that art plus the wild-fowl
# prompt (kept verbatim as the reference exemplar). Add every new prompt here.
#
# THE SIX ARE THE SIX THAT EXIST. A seventh block — HUD / ACTIVITY MARKS — sits at the
# BOTTOM of this file and is deliberately not listed above: it has no directory, no
# lookup script and no shipped PNG, only prompts. It is written down here because the
# reasoning behind each subject is the expensive part and it is settled; whoever picks
# up #581 or #249 should not re-derive it. Read its own preamble before generating one.
#
# ============================================================================
# PIPELINE
# ============================================================================
#
# 1. GENERATE at a large square size (1024x1024) on a plain solid MAGENTA
#    background. Any uniform background works — icon_key.py samples the four
#    corners — but magenta keys cleanest against earth-tone subjects.
#
#    ASK FOR NO TILE, AND CHECK THE COVERAGE NUMBER BEFORE ANYTHING ELSE. Current
#    image models read "flat vector game icon … on a magenta background" as a
#    request for an APP ICON and draw a dark rounded-square tile with the magenta
#    only as a surround. The key then does exactly what it is told — the corners
#    are magenta, so it keeps the TILE as the subject — and you get a dark chip
#    with a shrunken icon centred in it, which composites over the panel as a
#    visible box. Measured on the first `staple` pair: `icon_key.py` reported
#    **"subject covers 73.2% of the canvas"** where a bare wheat ear is ~15-20%.
#    That printed line is the cheapest detector there is, so read it every time —
#    a coverage figure far above the subject's honest share means something behind
#    the subject survived. The counter-clause is in every `crops/` prompt below
#    ("no rounded square, no tile, no card, no badge, no frame, no border … the
#    flat magenta touching the subject on every side") — carry it into any new
#    prompt rather than assuming "plain solid magenta background" is enough.
#
# 2. KEY OUT the background to transparency and normalize the framing:
#
#      python3 scripts/texture/icon_key.py in.jpg out.png --size 256
#
#    --size 256 matters: every shipped fauna sprite is 256x256 RGBA, and the
#    .import files set process/size_limit=64 against MapView's pinned
#    TEXTURE_FILTER_NEAREST. The script's own default is 512 — pass 256.
#    Other flags: --bg R,G,B (default: sampled from the corners), --lo (default
#    40) / --hi (default 90) bound the soft alpha ramp in 0-255 RGB distance.
#    Widen --hi if JPEG ringing leaves a halo; narrow it if thin features
#    (whiskers, antler tines) are being eaten. It trims and re-frames with a 6%
#    margin per side so the outline never touches the texture edge.
#
#    THE DEFAULTS ARE TOO TIGHT FOR AI-TOOL OUTPUT. Those defaults assume clean
#    input; generators hand back JPEGs, whose ringing around the subject leaves a
#    visible MAGENTA RIM on the keyed PNG. Measured on the seal render, residual
#    magenta fell 421 -> 325 -> 238 as --hi went 90 -> 130 -> 170. So for a JPEG:
#
#      python3 scripts/texture/icon_key.py in.jpg out.png --size 256 --lo 55 --hi 170
#
#    Always eyeball the keyed PNG for a pink rim before shipping it — a halo is
#    invisible at 1024 against magenta and obvious once composited over terrain.
#
#    THE .import SIDECAR GODOT GENERATES IS WRONG BY DEFAULT. It writes
#    mipmaps/generate=false and process/size_limit=0; every shipped icon needs
#    mipmaps/generate=true and process/size_limit=64. Correct the sidecar after the
#    first import and reimport, then diff it against a sibling (e.g. deer.png.import)
#    — it should match modulo the uid and the .ctex path hash.
#
#    JUDGE AT TRUE MARKER SIZE (24-41px), NEVER at 1024. The two seal candidates
#    inverted between the two: the one that looked bolder at full size lost its
#    head/snout separation when downscaled, and the sleeker one won. Downscale
#    candidates side by side against a shipped sprite before choosing.
#
#    A LOW, WIDE SUBJECT RENDERS SMALLER THAN ITS NEIGHBOURS, and that is correct.
#    The framing normalizes the silhouette's bounding box into the square, so a
#    hauled-out seal fills the width but only ~40% of the height and reads smaller
#    on the map than a tall quadruped. Preserving aspect is right; do not "fix" it
#    by cropping or stretching.
#
# 3. PLACE the PNG in the family's directory, named for its art KEY
#    (e.g. fauna/seal.png).
#
# 4. WIRE it. A fauna species needs BOTH tables or it silently draws an OS emoji:
#      - ui/FoodIcons.gd    HERD_SPECIES: "<keyword>": "<emoji>"   (the fallback)
#      - ui/FaunaSprites.gd SPRITE_PATHS: "<keyword>": SPRITE_DIR + "<file>.png"
#    The keyword is matched as a substring of the sim's `display_name`, longest
#    match first — so "Grey Seals" resolves via "seal". Aliases share art
#    (caribou -> reindeer.png, grouse -> fowl.png).
#
#    AN ALIAS IS ONLY HONEST WHEN NO ROSTER SPECIES IS BEHIND IT. `deer.png` once
#    served reindeer/caribou/elk/gazelle as well, and three of those four were real
#    entries in `fauna_config.json` — so Wild Elk, Wild Reindeer, Desert Gazelle and
#    Red Deer were four distinct species drawing one marker (issue #439). Before
#    aliasing a new keyword, check whether the sim actually ships a species under it;
#    if it does, it needs its own art. `caribou` survives as an alias precisely
#    because nothing in the roster is named that — it is a second English word for
#    the animal `reindeer` already covers.
#
# 5. VERIFY at TRUE marker size (10-41px), never in a fitted preview frame which
#    renders ~2.5x too big:
#      godot --path . res://tools/map_preview.tscn   ->  map_fauna_sprites.png
#
# ============================================================================
# HOUSE STYLE
# ============================================================================

# Every icon is a flat vector game marker, not an illustration. The skeleton:
#
#   Flat vector game icon of a <subject + its 2-3 identifying features>,
#   <calm neutral pose>, shown in side profile facing right, drawn as a bold
#   simplified silhouette with a thick dark charcoal outline around the entire
#   body, flat limited color palette in <two named tones> that stay clearly
#   lighter than the outline, simple two-tone shading, calm neutral facial
#   expression, crisp clean edges, centered with even padding on a plain solid
#   magenta background, the <subject> floating with no ground shadow and no
#   shadow beneath the feet, minimal graphic style that stays clearly readable
#   when scaled down to 24 pixels, strategy game map marker icon
#
# The load-bearing clauses — do not drop them:
#   * SIDE PROFILE FACING RIGHT — the whole set faces right; one that faces left
#     reads as broken in a roster row.
#   * THICK DARK OUTLINE — this is what keeps the silhouette legible at marker size
#     over any terrain, and it is the one clause every shipped icon has. Naming the
#     fills as lighter than the outline HELPS but is not strictly required: the
#     shipped deer and rabbit prompts omit it and read fine.
#   * NO GROUND SHADOW — a cast shadow keys out as a grey smear or survives as a
#     dirty fringe, and the marker is composited over terrain that supplies its own.
#   * READABLE AT 24 PIXELS — the real constraint. Fine texture, gradients and
#     background scenery all dissolve; ask for none of them.
#   * SOLID MAGENTA BACKGROUND — the chroma key depends on it being flat and uniform.
#   * CALM NEUTRAL POSE/EXPRESSION — these are map markers, not action art.
#

# ============================================================================
# FAUNA  ->  assets/icons/fauna/  (ui/FaunaSprites.gd)
# ============================================================================
#
# The KEY IS THE ART FILENAME STEM, not the sim's species name — a few species
# alias onto one file (see the notes). 21 files, 21 prompts, and — verified rather than
# asserted this time — **20 roster species resolving to 20 DISTINCT sprites**. The first 18
# arrived as follows
# (wolf.png landed with Predators Phase 1a — the first predator sprite; "Grey Wolf
# Pack" resolves via the `wolf` key, no longer the 🐺 emoji fallback. elk.png,
# reindeer.png and gazelle.png landed with issue #439, splitting the three roster
# species that had been drawing `deer.png`; steppe_runner.png and marsh_grazer.png
# landed with the same issue, and see the warning immediately below for why they
# were found late).
#
# ⚠ "COVERAGE IS COMPLETE" IS A CLAIM ABOUT THE SIM'S ROSTER, NOT ABOUT THIS FILE.
# It was written here — and in `FaunaSprites.gd`, and in `sprites-widgets.md` — while
# **Steppe Runners and Marsh Grazers were both drawing the 🦬 `HERD_DEFAULT` emoji**,
# and it had been false since those two species were added to `fauna_config.json`.
# The claim was checked the easy way, by confirming every key in `SPRITE_PATHS` had a
# PNG, which is a different question from every SPECIES having art and cannot detect a
# species the table has never heard of.
#
# The reason nothing caught it is structural and worth naming: **`SPRITE_PATHS` and
# `map_preview`'s `FAUNA_SPRITE_ROSTER` are both hand-written CLIENT-side lists, and
# the coverage frame enumerates the client's own table.** A species the table does not
# know is invisible to the frame by construction, so the frame cannot fail on it —
# exactly the blind spot that let the four cervids share one marker. The player-visible
# tell was that the OS emoji **faced LEFT** while every bundled sprite obeys the house
# style's "facing right" clause, which is a cheap thing to notice in a screenshot.
# `cargo xtask fauna-icon-guard` now cross-checks this against `fauna_config.json`, so
# the claim is verified rather than asserted. Re-run it after adding a species.
#
# ⚠ THE OTHER HALF OF #439 — three species were in it long after it closed, and `grouse.png`,
# `ibex.png` and `snow_hare.png` are what closed it. `fauna-icon-guard` NOW CHECKS THIS: it fails
# when two roster species resolve to the same PNG, naming both.
# `fauna-icon-guard` passes green while three roster species draw ANOTHER SPECIES' marker
# through an alias. That is not a hole in the guard's implementation, it is the question it
# asks: it walks each `display_name` to a sprite and stats the PNG plus its `.import`
# sidecar, so it verifies REACHABILITY — every one of these reaches a real file. It never
# asks whether two species reach the SAME file, which is the whole of what #439 was about,
# and no client-side check can, since the collision is only visible from the sim's roster.
#
#     Alpine Ibex        -> `ibex`   -> goat.png     shared with Crag Goats
#     Forest Grouse      -> `grouse` -> fowl.png     shared with Wild Fowl
#     Snow Hare Warren   -> `hare`   -> rabbit.png   shared with Rabbit Warren
#
# By the PIPELINE section's own rule — an alias is only honest when NO roster species is
# behind it — all three were dishonest. They now point at their own art, leaving
# `caribou -> reindeer.png`, `bison`/`buffalo -> aurochs.png` and `oxen -> cattle.png` as the
# honest aliases (nothing in the roster is named any of those).
#
# ⛔ THE CHECK THAT CATCHES THIS IS DISTINCTNESS, and it is an assertion now rather than a number
# to notice. CORRECTION to what this file said first: the guard did NOT print `20 species -> 20
# sprites` while the pairs were sharing — `reached_files` is a set, so it printed `20 species ->
# 17 sprites`. The count was right there on every run and nobody compared it to the roster size.
# That is the failure worth remembering: the signal existed and was unasserted. The guard now
# states the two as EQUAL and fails when they are not.
#
# THE FOUR CERVID-ISH SPECIES ARE THE SET MOST AT RISK OF READING AS ONE ANIMAL, so
# each prompt below names the ONE feature that separates it from the other three at
# 24px, and they are separated on SILHOUETTE first because hue cannot be trusted at
# that size over varied ground:
#   deer      — tall CROWNED antlers branching upward, head raised alert, mid-brown
#   elk       — antlers sweeping BACK past the shoulder, shaggy dark neck mane, pale rump
#   reindeer  — a forward BROW SHOVEL over the face, thick white neck ruff, stocky
#   gazelle   — HORNS not antlers (slim, ringed, lyre-curved), tiny body, dark flank bar
# A generated icon that could be swapped with another row of that list is a reject,
# whatever it scores on ground contrast.
#
# THE NO-TILE COUNTER-CLAUSE IS CARRIED BY `steppe_runner` AND `marsh_grazer` ALONE,
# and the other sixteen are the exception rather than the rule. The PIPELINE section
# says to carry it into any new prompt because "plain solid magenta background" alone
# reads to current models as a request for an APP ICON — measured at 73.2% coverage on
# the first `staple` pair. Every fauna prompt written before those two omits it,
# INCLUDING elk / reindeer / gazelle, whose renders happened to come back clean at
# 20-27% coverage; the clause is cheap insurance, not a fix for a bug those sixteen
# have. **Carry it into the next new prompt, and keep reading the coverage number
# either way** — that printed line is the actual detector, and no prompt clause
# replaces it.


# aurochs.png — Wild Aurochs / bison / buffalo (aliases share this art)
aurochs: Flat vector game icon of a wild aurochs, a massive shaggy wild ox with a high shoulder hump and long forward-curving horns, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in deep warm brown earth tones that stay clearly lighter than the outline, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the animal floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

boar: Flat vector game icon of a wild boar standing in a calm neutral pose, with humped bristly shoulders, a short thick snout and two prominent curved white tusks, short sturdy legs, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in medium warm brown earth tones that stay clearly lighter than the outline, simple two-tone shading, the back bristles drawn as a few chunky simple spikes rather than many fine hairs, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the boar floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# catfish.png — Silt Catfish (the delta/littoral resident shoal)
# MUST NOT LOOK LIKE sites/fish.png, which is a plump COOL BLUE-GREY river fish standing for the
# riverine_delta gathering site — the two can appear on the SAME hex (a delta tile with a shoal on
# it), so this is the fauna/seal.png vs sites/seal.png situation again: two files, deliberately
# different art, neither a stale copy of the other. Separated on THREE axes, all of which survive
# downscaling: WARM dark olive-brown vs the site fish's cool silver; a broad FLATTENED head with a
# wide downturned mouth vs a rounded snout; and the barbels, which the site fish does not have.
# THE BARBELS ARE THE HAZARD. Fine whiskers are exactly what `icon_key.py`'s alpha ramp eats (see
# the --lo/--hi note in the pipeline) and what dissolves at 24px, so the prompt asks for a FEW THICK
# ones — the same "chunky forms over fine detail" dodge the mammoth's fur and the boar's bristles use.
# If they still key out, narrow --hi rather than thinning them further.
#
# ---------------------------------------------------------------------------------------------
# V1 SHIPPED AND FAILED IN PLAY: "it blends in a lot with the background tile, almost invisible."
# The lesson generalizes past this icon, so it is recorded here rather than in a commit message.
#
# V1 asked for "warm dark olive-brown and muddy ochre" — which is, near enough, THE COLOUR OF THE
# GROUND IT SPAWNS ON. Measured (Rec.709 mean luma of the opaque fill vs the base textures of the
# terrains this species actually sits on): catfish fill **97**, against alluvial_plain 53,
# floodplain 59, rocky_reg **80**, navigable_river 89. A ~17-point gap against rocky ground is not
# a marker, it is camouflage. Ranked against the whole fauna set by mean |sprite − its terrains|,
# v1 came third from the BOTTOM (33.8, vs seal 77, sheep 99, cattle 112).
#
# TWO RULES FALL OUT OF THAT, and they apply to every future species:
#   1. THE HOUSE STYLE'S "lighter than the outline" IS NOT THE TEST THAT MATTERS. V1 passed it
#      comfortably (fill/outline separation 63, mid-pack — better than aurochs' 30). The test that
#      matters is contrast against the GROUND, and nothing in the style skeleton enforces it.
#      Before generating, look up what the species' `host_biomes` terrains actually look like.
#   2. HUE COLLISION COMPOUNDS IT. Brown-on-brown reads worse than the luma gap predicts, and this
#      subject has no protruding features to carry a silhouette the way antlers, ears or a comb do
#      — a fish is a smooth lozenge, so tone is doing ALL the work.
# The fix is NOT "add an outline" (it already has a thick one) — it is to move the BODY into a
# different tonal register from the earth: pale sandy flanks, cream belly, dark back for the
# two-tone read. Target fill luma ~140, i.e. the goat/seal band. The dark dorsal is what keeps it
# from collapsing into sites/fish.png's cool silver, so it must stay.
#
# V2 (the prompt below) SHIPPED and measures: fill luma **160**, mean |fill − host ground| **93.0**
# (worst case 48.4) — against v1's 97 / 34.8 / **8.3**, and comfortably past the seal's 90.8 / 46.2.
# Confirmed at 24/32/41px over all four real host grounds before shipping.
#
# KEYED AT `--lo 85 --hi 240`, NOT the pipeline's JPEG default of `--lo 55 --hi 170`:
#   python3 scripts/texture/icon_key.py in.jpg out.png --size 256 --lo 85 --hi 240
# THE PALE BODY IS EXACTLY WHY. This is the **white-species hazard** (see the sheep/fowl note in
# the fauna header) showing up in the KEYER rather than on the terrain: `icon_key.py` decides alpha
# by RGB DISTANCE FROM THE BACKGROUND, and a cream/buff subject sits far closer to magenta than a
# dark olive one, so the same ramp that was clean for v1 left a visible VIOLET FRINGE along the jaw
# and barbels of v2. Measured residual magenta fell 258 → 224 → 104 across the ramp; opaque pixels
# dropped only ~2% (11933 → 11678), i.e. the barbels survived — which is the trade to check, since
# widening the ramp is exactly what eats thin features. **Judge the fringe at 4x magnification over
# a MID-GREY field**, not on the magenta plate and not at 1:1: at 1:1 the violet edge is invisible,
# and against magenta it is invisible by definition.
# GENERAL RULE — **CORRECTED, and the original cost an icon nearly getting keyed to death** (issue
# #439, measured while keying the elk). This note used to read "the paler the subject, the wider the
# keying ramp it needs". That is the wrong variable. `icon_key.py` sets
# `alpha = smoothstep(lo, hi, dist)` where `dist` is EUCLIDEAN RGB DISTANCE FROM THE KEY COLOUR, so
# what decides the safe `--hi` is the subject's **hue proximity to the key**, not its LIGHTNESS.
# Distance from magenta is large for very dark AND very pale palettes and SMALLEST for warm
# mid-tones — tan, rufous, red-brown — which are the neighbours of magenta in RGB.
#
# Measured over the whole shipped fauna set (minimum distance-from-magenta across each sprite's
# fully-opaque pixels): fowl 103, wolf 112, cattle 122, deer 123, boar 124, aurochs 130, mammoth 132,
# seal 140, sheep 141, **catfish 151 — the FARTHEST of the set**. The catfish could afford `--hi 240`
# because its cool sandy-and-olive palette sits farther from magenta than anything else here, and
# being pale is merely correlated with that, not the cause. Note that the DEFAULT `--hi 170` is
# already above most of those minima, i.e. every shipped icon's warmest pixels are inside the ramp
# and slightly soft. That is the normal, accepted state.
#
# SO: `--hi` MUST STAY BELOW THE SUBJECT'S OWN MINIMUM DISTANCE, and raising it blindly destroys the
# icon rather than merely softening it. Measured on the elk candidate at fixed `--lo`: `--hi` 170 →
# 200 → 230 dropped fully-opaque pixels 22208 → 11469 → 6691, i.e. it dissolved HALF the animal —
# and raising `--lo` instead is no escape, since 55 → 70 moved ~5800 pixels out of opaque into the
# semi-transparent rim, eating the thick dark outline that is the house style's one load-bearing
# clause. The elk therefore shipped at the plain JPEG default.
#
# THE CHEAP CHECK, before touching either threshold: measure the subject's minimum
# distance-from-background over its opaque pixels, and compare the residual against THE SHIPPED SET
# rather than against an absolute — 3 (deer) to 209 (cattle) is the real band, median ~59, so a
# candidate scoring 52 needs no intervention at all. **A residual that merely LOOKS bad at 4x
# magnification is not evidence**; every icon in this directory has that faint violet rim.
#
# AND IF A SUBJECT IS GENUINELY TOO CLOSE TO THE KEY, MOVE THE KEY, NOT THE RAMP. `icon_key.py`
# samples the background from the four corners and `--bg` overrides it, so a warm rufous subject can
# be generated on a saturated CYAN or GREEN plate instead — worth ~240 distance against magenta's
# ~130 for the same tan, i.e. roughly double the margin, with no ramp change and no eaten outline.
# ---------------------------------------------------------------------------------------------
catfish: Flat vector game icon of a river catfish with a broad flattened head, a wide downturned mouth and four thick barbels sweeping back from its snout, a long tapering body with low-set fins and a rounded tail, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette with pale sandy-buff flanks and a bright cream underside beneath a narrow dark olive back, the body reading clearly PALE and light overall so it stands out sharply against dark brown earth and muddy riverbank ground, strong two-tone contrast between the light flanks and the dark dorsal stripe, the barbels drawn as a few chunky simple tapering strokes in the same pale tone rather than fine whiskers, calm neutral expression, crisp clean edges, centered with even padding on a plain solid magenta background, the fish floating with no ground shadow and no shadow beneath the body, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# cattle.png — the domestic ox; keyword aliases `cattle` + `oxen`
cattle: Flat vector game icon of a domestic ox, a stocky heavy-bodied cow with short blunt horns and a straight back, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in light tan and cream earth tones with simple two-tone shading, clearly lighter overall than a dark wild ox, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the animal floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# deer.png — Red Deer ONLY since issue #439 (it served elk / reindeer / caribou / gazelle before)
# host_biomes temperate_forest / mixed_woodland / savanna_grassland — grounds measuring luma 32–125,
# i.e. the DARK-to-MID half of the map. Its fill measures 87, which is why this prompt has survived
# unchanged: it is the one cervid sitting on ground its own tone separates from. Do not lighten it to
# "match" the three new siblings — they are pale because THEIR ground is pale.
deer: Flat vector game icon of a wild red deer stag with large branching antlers, a slender body and long legs, head raised alert, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in warm earth tones with simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, the deer floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# elk.png — Wild Elk (issue #439; drew deer.png until then)
# SEPARATION FROM deer.png IS THE POINT OF THIS ICON, and it is carried by three things that all
# survive 24px: antlers that sweep BACKWARD along the spine (the red deer's branch UPWARD into a
# crown), a shaggy dark neck mane hanging under the throat, and a pale rump patch. Head carried
# LEVEL, not raised — the deer prompt spends "head raised alert", so this one must not.
# GROUND-CONTRAST HAZARD, and it is the widest in the set: host_biomes is boreal_arctic ALONE, whose
# four terrains measure luma 40 (boreal_taiga), 117 (tundra), 146 (periglacial_steppe) and 198
# (seasonal_snowfield) — a 158-point span, so NO single fill tone clears all four. The wolf's answer
# applies verbatim: force a strong two-tone split (dark body over a pale rump/underside) so whichever
# half loses to the ground, the other half carries the silhouette. Judge at 24px over BOTH dark taiga
# and bright snowfield before shipping; a mid-brown elk that reads fine on tundra is not verified.
elk: Flat vector game icon of a wild elk, a large heavy-bodied deer with a thick shaggy dark mane hanging beneath its throat, tall sturdy legs and long antlers that sweep BACKWARD over the shoulders in a single long beam with short upward tines rather than branching up into a crown, head carried level in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette with a deep chocolate-brown neck and mane and a mid tawny-brown body that stays clearly lighter than the outline, and a distinctly PALE cream rump patch and pale underside, strong two-tone contrast between the dark neck and the pale rump so the shape reads over both dark forest ground and bright snow, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the elk floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# fowl.png — Wild Fowl. IT ALSO SERVES `grouse` TODAY, and that is the collision in the
# header warning: Forest Grouse is its own roster species. `grouse.png` below replaces it.
fowl: Flat vector game icon of a wild jungle fowl rooster with an upright arched tail, a red comb and wattle, and a compact rounded body, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in warm copper brown and dark bronze tones that stay clearly lighter than the outline, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the bird floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# gazelle.png — Desert Gazelle (issue #439; drew deer.png until then)
# SEPARATION FROM deer.png is the easiest of the three, and it is anatomical rather than stylistic:
# a gazelle wears HORNS, not antlers — slim, ringed, curving back in a shallow lyre — and it is
# TINY (body_mass 4 against the red deer's 60), so the proportions do the rest: short body, very
# long thin neck and legs. Nothing on this animal branches. If the render branches, reject it.
# GROUND-CONTRAST HAZARD — the catfish lesson, inverted: host_biomes is semi_arid_scrub, whose
# terrains measure luma 178 (hot_desert_erg), 197 (salt_flat) and 80 (rocky_reg). THE OBVIOUS
# COLOUR IS THE WRONG ONE — a sandy-buff gazelle is within ~20 of two of its three grounds and is
# camouflage by construction, which is exactly how catfish v1 shipped and failed. The gap between
# rocky_reg (80) and the pale pair (178/197) is where a fill has to sit, so aim for a MID rufous
# tan around luma 125–135 (the goat's band) and make the flank stripe carry the rest: real gazelles
# wear a hard dark chocolate bar along the flank above a white belly, which is a horizontal
# high-contrast edge — the one kind of detail that survives downscaling. It is doing the same job
# the catfish's dark dorsal does, and it must stay.
# KEYING — THIS IS THE MOST KEY-SENSITIVE ICON IN THE DIRECTORY, and the two hazards pull opposite
# ways, which is the whole difficulty of it. The ground-contrast rule above demands a warm mid
# rufous-tan; the corrected GENERAL RULE (catfish note) says warm mid-tones are exactly the palette
# CLOSEST to magenta in RGB — a representative rufous tan measures ~130 from it, at or below the
# lowest minimum in the shipped set (fowl, 103) and below the default `--hi` of 170. So key it at the
# plain default, do NOT widen the ramp under any circumstances, and measure the minimum
# distance-from-magenta over the opaque pixels before shipping.
# IF IT FRINGES ANYWAY, MOVE THE KEY RATHER THAN THE RAMP: regenerate on a saturated CYAN or GREEN
# plate (same prompt, swap the background clause) — the same tan measures ~240 from cyan, roughly
# double the margin — and let `icon_key.py` sample it from the corners as usual. That is the
# sanctioned escape hatch for a subject whose required colour collides with the key.
# WHAT ACTUALLY HAPPENED: the escape hatch was NOT needed. The shipped render measures min
# distance-from-key **140** with a residual of **18** — mid-pack for the set and better than deer
# (123) or cattle (122) — so it keyed at the plain default like everything else. The warning above is
# still worth its space, because it is a property of the REQUESTED PALETTE rather than of this one
# render, and the next rufous subject re-rolls the same dice. Keep it as a thing to MEASURE, not as a
# known defect: the check is one number, and it came back fine.
gazelle: Flat vector game icon of a desert gazelle, a small slender antelope with a short compact body, a long thin neck, long delicate legs and a pair of slim upright ringed HORNS curving gently backward in a shallow lyre shape, never branching antlers, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in a warm mid rufous-tan that stays clearly lighter than the outline but distinctly DARKER than pale desert sand so the shape stands out over bright sand and salt flats, a bold dark chocolate-brown horizontal stripe running along the flank, a crisp white belly and a white stripe on the face, strong two-tone contrast between the tan back and the white underside, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the gazelle floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# goat.png — Crag Goats. IT ALSO SERVES `ibex` TODAY, and that is the collision in the
# header warning: Alpine Ibex is its own roster species. `ibex.png` below replaces it.
goat: Flat vector game icon of a wild goat, a small slender animal with long backward-sweeping curved horns and a pointed chin beard, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in warm sandy tan earth tones that stay clearly lighter than the outline, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the animal floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# grouse.png — Forest Grouse. STATUS: SHIPPED. Keyed at the plain JPEG default
# (`--size 256 --lo 55 --hi 170`); coverage 30.4%, magenta residual **8** — near the clean end
# of the shipped band (0 catfish/elk/gazelle .. 151 ibex, median 11).
# It exists to split `grouse` off `fowl.png`, which Wild Fowl also holds — the header warning
# has the collision. THE SEPARATION IS ANATOMICAL, because both are compact brown-ish birds and
# hue cannot be trusted at 24px: `fowl.png` is a ROOSTER, identified by a red comb and wattle and
# an upright arched tail. This one has NEITHER, and its tail is BROAD, FANNED and carried LOW.
# A render that grows a comb is a reject however well it scores on anything else.
# GROUND CONTRAST — host_biomes `mixed_woodland`, whose terrains are the DARKEST half of the atlas:
# mixed_woodland 48, peat_heath 31, impact_crater_field 102 (Rec.709 mean luma of the base
# textures). So this is the catfish lesson run the other way — the obvious brown game bird is
# camouflage here, and the fill has to sit PALE, around luma 150-175, with the barring carrying
# the two-tone read. Judge it at 24px over peat heath before shipping; a bird that reads on the
# crater field is not verified.
# KEYING — a cream/oatmeal subject sits FAR from magenta in RGB (the catfish's corrected rule:
# what decides `--hi` is hue proximity to the key, not lightness), so it can afford a wide ramp.
# Measure the minimum distance-from-magenta over the opaque pixels first; if it fringes, widen
# toward `--lo 85 --hi 240` rather than reaching for the cyan plate.
# THE CHOSEN RENDER — of two candidates the pick was decided by BAR COUNT, which is the same
# call the mammoth's fur and the boar's bristles already make. The rejected one wore ~10 thin
# scalloped crescents down the flank and fine feather-split lines in the tail; at 24px those
# average into a flat mid-tone smear, which costs the bird BOTH of its reads at once — the
# barring stops being barring and the pale fill it was supposed to contrast with goes muddy.
# The chosen render carries ~5 thick chocolate blocks and a tail banded in 3 bold stripes, so
# what survives downscaling is a pale blob with two or three dark blocks in it, which is
# exactly the amount of structure 24px can hold. Its tail is also the larger of the two, and
# the tail is what carries the separation from `fowl.png`'s upright arched rooster tail.
# ⚠ EXPECT A HIGH COVERAGE NUMBER AND DO NOT READ IT AS THE APP-ICON FAILURE. This is a plump
# bird drawn large in frame with a broad fanned tail, so it honestly occupies far more of the
# canvas than the ~15-20% a wheat ear does. The tell for the tile failure is a DARK ROUNDED
# RECTANGLE behind the subject, not the percentage on its own — look at the keyed PNG.
grouse: Flat vector game icon of a forest grouse, a plump rounded ground bird with a small smooth head, a short curved beak, NO comb and NO wattle at all, a heavy compact body carried low on short feathered legs, and a broad fanned tail spread horizontally behind it and held LOW rather than raised, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in PALE cream and light oatmeal buff with a few chunky darker chestnut bars across the breast and tail, the bird reading clearly LIGHT and pale overall so it stands out sharply against dark forest floor and near-black peat ground, strong two-tone contrast between the pale body and the darker bars, no fine feather detail, calm neutral expression, crisp clean edges, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the bird on every side, the grouse floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

horse: Flat vector game icon of a wild horse with a short upright bristly mane, a compact sturdy body and a flowing tail, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in dun and warm grey-brown earth tones that stay clearly lighter than the outline, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the horse floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# ibex.png — Alpine Ibex. STATUS: SHIPPED. Keyed at the plain JPEG default
# (`--size 256 --lo 55 --hi 170`); coverage 31.4%, magenta residual **151** — the HIGHEST in the
# directory, and left alone deliberately: `cattle.png` shipped at 149 and the file's own rule is
# that a residual is judged against the shipped band, not against how it looks at 4x. Widening
# the ramp to chase it would eat the horn ridges, which are the whole icon.
# It exists to split `ibex` off `goat.png`, which Crag Goats also holds — the header warning has
# the collision. THE SEPARATION IS ANATOMICAL AND PROPORTIONAL, because both are horned caprids:
# `goat.png` is SMALL AND SLENDER with a chin beard and slim backward-sweeping horns. This one is
# STOCKY — deep chest, short thick legs, body_mass 10 against the crag goat's 6 — and its horns are
# MASSIVE scimitars, longer and thicker than its own head, carrying a few BOLD transverse ridges.
# The ridges must be chunky notches, not fine lines: this is the same "chunky forms over fine
# detail" dodge the mammoth's fur and the boar's bristles use, and for the same reason.
# GROUND CONTRAST — host_biomes `montane_highland` is the BRIGHTEST ground any species stands on:
# karst_highland 139, high_plateau 155, alpine_mountain 170. A sandy-tan ibex is camouflage by
# construction, which is exactly how catfish v1 shipped and failed, so the body sits DARK
# (chocolate/slate, target fill ~60-80) with a pale belly and rump carrying the two-tone read.
# ⚠ THE HORNS ARE THE ONE THING TO MEASURE, and it is not the body that is at risk. They are the
# largest element in the silhouette and they are drawn in PALE grey-tan, which is within ~30 of
# alpine rock. What saves them is structure rather than tone — the thick charcoal outline plus the
# bold ridge notches read as a striped arc against flat stone. Confirm that at 24px over
# alpine_mountain specifically, not over the darker two.
# THE CHOSEN RENDER — of two candidates, the ONE-HORN version won over the two-horn one on both
# counts that decide an icon at marker size: a single fat ridged arc keeps its notches when
# downscaled where two slimmer horns merge into a blur, and the stockier body is the whole of the
# separation from `goat.png`, which the longer-legged candidate gave back.
ibex: Flat vector game icon of an alpine ibex, a stocky heavy-bodied wild goat with a deep chest, a short thick neck and short sturdy legs, carrying a pair of MASSIVE backward-arching scimitar horns that are longer and far thicker than its own head, each horn marked with a few bold chunky transverse ridges rather than many fine lines, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in DARK chocolate brown and cool slate grey-brown that reads clearly DARKER than pale grey mountain rock and bright alpine stone, with a distinctly pale sandy belly and rump for strong two-tone contrast, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the animal on every side, the ibex floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

mammoth: Flat vector game icon of a woolly mammoth standing in a calm neutral pose, with two huge curved tusks, a domed head, a raised curled trunk and shaggy hanging fur, sturdy column legs, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in medium warm brown earth tones that stay clearly lighter than the outline, simple two-tone shading, the shaggy fur drawn as a few chunky simple locks rather than many fine hairs, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the mammoth floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# marsh_grazer.png — Marsh Grazers (an INVENTED species; no real animal to copy)
# 100 kg pastoral migratory grazer of `wetland_swamp` + `riverine_delta`. Read it as a WADING
# grazer: high rump, long legs made for standing in water, splayed spreading hooves, and long
# gently-spiralled horns — the lechwe/sitatunga build. It must NOT read as `aurochs.png` (the heavy
# short-horned bovines) or `cattle.png`; the high rump and the long legs are what separate it.
# GROUND-CONTRAST HAZARD, and here it decides the palette outright: every one of this species'
# grounds is DARK — hydrothermal_vent_field 34, freshwater_marsh 40, alluvial_plain 53, floodplain
# 59, navigable_river 89, inland_sea 90. There is no bright ground anywhere in its range, so the
# animal must be PALE, and the real-world analogues are the wrong guide — a chestnut lechwe would
# measure ~90 and sit exactly on top of navigable_river, which is catfish v1's failure repeated
# knowingly. Target a fill around 190: that clears the darkest ground by ~155 and the lightest by
# ~100. Dark lower legs are the one place to spend the dark tone, and they read as the waterline.
# KEYING: pale-and-cool is FAR from magenta (a cream measures ~230), so unlike the gazelle this one
# has real headroom — a wider ramp is available if JPEG ringing leaves a fringe. Measure the min
# distance-from-key first, as always, rather than assuming either direction.
#
# ⚠ DO NOT PUT THE ANIMAL IN WATER, and this prompt used to. It read "standing calmly ankle-deep as
# if in shallow water", which CONTRADICTED its own "floating with no ground shadow" clause eleven
# words later — and the generator obeyed the first one, drawing water RIPPLE RINGS around the hooves
# in both renders. That is a ground element, which is precisely what the house style's no-shadow
# rule exists to exclude, and it failed exactly the way that rule predicts: at 24px the rings
# collapsed into a dirty horizontal bar under the feet, carrying a visible PINK CAST on
# alluvial_plain and navigable_river because thin pale strokes against magenta are almost all edge.
# Measured, the rings held most of the icon's key fringe — residual **73 with them, 45 without**.
# The waterline is carried by the DARK LOWER LEGS alone, which is enough and costs nothing.
#
# THE SHIPPED PNG IS POST-PROCESSED, so it is not a pure `icon_key.py` output and re-running the
# prompt will not reproduce it byte-for-byte: the ripple band was cut (every row below the hooves
# dropped) and the result re-trimmed and re-framed the way `icon_key.py`'s `_fit` does. Connected-
# component stripping was tried FIRST and does not work — the rings are drawn TOUCHING the hooves,
# so only 158 px of 4 detached arcs separate out and the bar survives. Cutting also made the icon
# BETTER, not merely cleaner: the bbox shrank, so the reframe scaled the animal up ~8% (14050 →
# 15234 opaque px) and it reads larger at marker size. **With the clause above removed, a fresh
# render should need none of this** — check for rings before assuming it does.
marsh_grazer: Flat vector game icon of a marsh grazer, a large pale wading antelope with a high arched rump, a deep chest, long slender legs with wide splayed hooves, and a pair of long gently spiralling horns sweeping back from its head, standing calmly in a neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in pale cream and light silver-grey that stays clearly lighter than the outline and reads clearly LIGHT overall so it stands out sharply against dark swamp water, black peat and wet river mud, with distinctly darker charcoal lower legs below the knee suggesting a waterline, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background with the flat magenta touching the subject on every side, no rounded square, no tile, no card, no badge, no frame, no border and no background panel of any kind, the animal floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

rabbit: Flat vector game icon of a wild brown rabbit with long upright ears, a compact crouched body and a short round tail, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in warm earth tones with simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, the rabbit floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# reindeer.png — Wild Reindeer, and the `caribou` alias (issue #439; both drew deer.png until then)
# SEPARATION FROM deer.png AND elk.png both matter here, since all three are antlered and two of
# them share boreal_arctic ground. The reindeer's own mark is the BROW SHOVEL — the flattened
# palmate tine that hangs forward over the face — which no other cervid in the set has and which
# reads at 24px as a distinct blob in front of the muzzle. Behind it: a thick white neck ruff and a
# stocky, short-legged build (body_mass 18, against the elk's 40 and the deer's 60), so it must not
# be drawn tall and rangy like either.
# GROUND-CONTRAST HAZARD: host_biomes boreal_arctic + montane_highland — luma 40 (boreal_taiga), 117
# (tundra), 146 (periglacial_steppe), 198 (seasonal_snowfield), 139 (karst_highland), 155
# (high_plateau), 170 (alpine_mountain). Six of the seven are PALE, so unlike the elk this one does
# not need a mid-tone compromise: go dark on the body and let the white ruff be the accent. A pale
# grey-white reindeer — the obvious real-world colour — would vanish on snow and rock alike.
# KEYING: use the plain JPEG default, `--lo 55 --hi 170`. An earlier draft of this note said to key
# the white ruff at `--lo 85 --hi 240` "because a cream subject sits close to magenta" — that is the
# error the corrected GENERAL RULE in the catfish note above documents, and here it would have been
# actively destructive: this animal is specified DARK grey-brown over most of its area, and a `--hi`
# that high dissolves a dark body (measured on the elk: half the opaque pixels gone). The white ruff
# is far from magenta, not close to it. Measure the min distance-from-magenta and compare the
# residual against the shipped band before changing either threshold, and check the brow shovel's
# tines survived — they are the one feature here that a widened ramp would eat.
reindeer: Flat vector game icon of a wild reindeer, a stocky short-legged deer with a deep chest, a thick shaggy white ruff of fur around its neck and throat, and large antlers of which the front tine is a broad flattened shovel hanging FORWARD over its face, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette with a dark cool grey-brown back and flanks that stay clearly lighter than the outline, contrasted against a bright creamy white neck ruff, white underside and white lower legs, strong two-tone contrast between the dark body and the white ruff so the shape reads over pale snow and pale grey rock, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the reindeer floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# seal.png — fauna/seal.png — NOT the same image as sites/seal.png
seal: Flat vector game icon of a grey seal with a sleek rounded torpedo body, a short whiskered muzzle, a large dark round eye, short front flippers and paired rear flippers, hauled out resting on its belly with its head and chest raised in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in cool slate grey and soft pale silver tones that stay clearly lighter than the outline, simple two-tone shading with a lighter underside and a few subtle darker mottled spots along the back, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the seal floating with no ground shadow and no shadow beneath the body, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

sheep: Flat vector game icon of a sheep with a thick rounded woolly fleece, a small dark face and dark slender legs, standing in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette using a soft cream and warm grey fleece that is clearly darker than pure white, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the sheep floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# snow_hare.png — Snow Hare Warren. STATUS: SHIPPED on v2. Keyed at the plain JPEG default
# (`--size 256 --lo 55 --hi 170`); coverage 23.0%, magenta residual **105**, against
# `rabbit.png`'s 106 — the icon it exists to stop sharing, and the same neighbourhood of the
# band. The wide ramp the pale v1 would have needed was never required.
# It exists to split `hare` off `rabbit.png`, which Rabbit Warren also holds — the header warning
# has the collision. Separation from the rabbit is size, limb and COLOUR TEMPERATURE: this animal
# is longer-bodied and lankier, with longer ears and powerful elongated hind legs, and it is COOL
# grey-white where the rabbit is warm brown.
# ⚠ THE HARDEST GROUND IN THE ROSTER, and the reason this prompt does not simply say "white".
# host_biomes `boreal_arctic` spans FOUR terrains whose luma covers almost the whole range the
# atlas has: boreal_taiga 40, tundra 117, periglacial_steppe 146, seasonal_snowfield 198. NO FILL
# TONE WORKS ON BOTH ENDS — a white hare disappears on the snowfield and a dark one disappears in
# the taiga, so the usual "pick a tone away from the ground" move has nowhere to go.
# THE ANSWER IS THE GAZELLE'S, NOT THE CATFISH'S: sit the body MID-RANGE (~120-135, the goat/seal
# band, splitting the 40/198 spread) and let a hard high-contrast DETAIL carry the read where the
# tone cannot — solid BLACK EAR TIPS, doing exactly the job the gazelle's dark flank bar does on
# bright sand. The ear tips are load-bearing; a render that softens them to grey is a reject.
# KEYING — a pale cool subject sits far from magenta, so a wide ramp is affordable, but the ear
# tips and ear edges are the thin features a wide ramp eats. Measure the opaque-pixel minimum
# first and prefer the plain JPEG default unless the residual demands more.
#
# ---------------------------------------------------------------------------------------------
# V1 REJECTED BEFORE KEYING — and it was rejected on a NUMBER, which is the point of writing it
# down. Both v1 candidates came back a pale silver: dominant fill luma **209** and **205**
# against the prompt's stated target of 120-135. That is not merely off-target, it is the single
# WORST place on the scale, because `seasonal_snowfield` measures 198 — the body was landing
# ON TOP of the brightest ground it stands on. Scored the way the catfish note scores a fill
# (|fill − ground| across the species' real host terrains):
#
#     v1 #1  fill 209 -> taiga 169, tundra 92, periglacial 63, snowfield **11**
#     v1 #2  fill 205 -> taiga 165, tundra 88, periglacial 59, snowfield **7**
#
# Catfish v1 shipped at a worst case of 8.3 and came back from play as "almost invisible".
# These are 11 and 7. Nothing about the renders was wrong otherwise — pose, ears, black tips and
# outline were all as asked — which is exactly why the measurement is the gate and not the look
# of the render on the magenta plate.
#
# ⚠ THE FIX IS TO WIDEN THE SPLIT, NOT TO PICK A BETTER SINGLE TONE, and this species is the
# reason that distinction matters. Its four grounds (40 / 117 / 146 / 198) are spread so widely
# that NO single fill can clear all of them — the best any one tone can do is a worst case in the
# high 30s. A two-tone body escapes the problem instead of optimising inside it, because each
# ground only needs ONE of the two halves to separate from it. Scoring the better half per
# ground: v1's 209-back/248-belly gives a worst case of **50** (the snowfield, carried by the
# belly alone); a **78-back/248-belly** gives **102**, and every ground then has a half that
# works. That is the elk's rule — "whichever half loses to the ground, the other half carries
# the silhouette" — applied to the widest ground spread in the roster.
# So v2 asks for a MEDIUM-DARK SLATE back over a crisp WHITE underside, and names the failure
# mode explicitly ("never pale silver") because that is what v1 defaulted to twice.
# GEOMETRY TO KEEP FROM V1: the wider-eared candidate, whose ears sat in a broad V with the
# black tips as chunky blocks. The lankier one drew them slimmer and more parallel, which at
# 24px merges two thin dark slivers into one blob and loses the count.
#
# V2 SHIPPED THE FIX, and the number it was aimed at came back exactly. Measured on the two v2
# candidates: slate back luma **87** and **93** over a 248 belly, i.e. a two-tone split of 155-161
# against v1's ~39. Scored by the best half per ground:
#
#     ground                back gap   belly gap   best half
#     boreal_taiga    40        47        208         208
#     tundra         117        30        131         131
#     periglacial    146        59        102         102   <- the binding one
#     snowfield      198       111         50         111
#
# WORST CASE **102**, against v1's 50 — and note WHICH ground now binds. It is no longer the
# snowfield (the failure this arc was about, now at 111) but periglacial_steppe at 146, which
# sits between the two body tones and so is the one ground neither half runs away from. That is
# the residual cost of a two-tone answer and it is the right cost to pay: 102 is twice the
# margin any single fill could have bought here.
#
# THE CHOSEN RENDER — the two v2 candidates measured within noise of each other (back 87 vs 93,
# identical worst case, ear-tip black mass 47% vs 46% of the tip zone), so TONE DID NOT DECIDE IT
# and neither did the ear tips. The LANKIER candidate won on SILHOUETTE, which is the file's own
# tie-break: longer legs, longer ears, a taller narrower body. Separation from `rabbit.png` is
# the entire reason this file exists, `rabbit.png` is drawn as a COMPACT CROUCHED body, and the
# stockier candidate was moving back toward it. At 24px what survives is a tall narrow two-tone
# shape with two long ears, which cannot be confused with the rabbit's low lump.
# ---------------------------------------------------------------------------------------------
snow_hare: Flat vector game icon of a snow hare, a long-bodied arctic hare much larger and lankier than a rabbit, with very long BROAD upright ears set in a wide V, powerful elongated hind legs and an alert upright sitting posture, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette with the back, haunch, flanks and outer ears in a MEDIUM-DARK COOL SLATE GREY the tone of wet stone, clearly and obviously darker than the animal's own white belly and much darker than snow, never pale silver and never near-white and never light grey, over a crisp bright WHITE chest, belly, inner ear lining and tail, a strong wide-range two-tone split between the dark slate back and the white underside so that against dark ground the white belly carries the shape and against bright snow the dark back carries it, and a hard solid BLACK tip on each ear drawn as a chunky solid block, simple two-tone shading, calm neutral facial expression, no fine fur detail, crisp clean edges, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the animal on every side, the hare floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# steppe_runner.png — Steppe Runners (an INVENTED species; no real animal to copy)
# 120 kg pastoral migratory grazer of `savanna_grassland`. Read it as a fast open-country runner —
# the saiga build: lean and deep-chested on long thin legs, a distinctive bulbous down-turned
# muzzle, and short ringed horns rising nearly vertical. The muzzle and the vertical horns are what
# carry it at 24px. It must NOT read as `horse.png` (the only other big open-grassland grazer) —
# horses have no horns and a flowing tail, so keep the horns crisp and the tail a short tuft — nor
# as `sheep.png`, the set's other pale animal, which is a rounded fleece blob with no legs to speak
# of; this one is all leg.
# GROUND-CONTRAST HAZARD: its three grounds cluster in the MIDDLE — oasis_basin 71, prairie_steppe
# 110, semi_arid_scrub 125 — which is the worst possible arrangement for a mid-toned animal. The
# gaps between them are only ~20 and ~8 wide, so ANY mid-brown fill lands within a hair of one of
# them; there is no safe middle to aim at. The escape is to leave the middle entirely, and PALE is
# the right direction (dark would fight the thick charcoal outline for the same tonal space). At a
# fill around 200 the gaps are ~129 / ~90 / ~75 — comfortable on all three — and it matches what a
# real saiga looks like anyway.
# KEYING: same headroom note as `marsh_grazer` — pale-and-cool sits far from magenta, so a wider
# ramp is available if needed. Measure the min distance-from-key rather than assuming.
# NEAREST LOOKALIKE IS `gazelle`, AND THIS PAIR SEPARATES ON COLOUR RATHER THAN SHAPE — the one
# place in the set where that is true, so it is worth knowing before regenerating either. Measured
# on the shipped art their proportions are nearly the same (aspect 0.93 vs 0.78, leg fraction 29%
# vs 30%), and what carries the read is saturation 0.27 vs 0.46 and luma 174 vs 129 — a flat sand
# runner against a saturated orange-rufous gazelle with a bold white flank stripe — plus head
# carriage, this one's muzzle drooping where the gazelle's head is high and level.
# **The practical risk is low and the reason is worth recording**: their host modules are DISJOINT
# (`savanna_grassland` → prairie_steppe / semi_arid_scrub / oasis_basin; the gazelle's
# `semi_arid_scrub` → hot_desert_erg / rocky_reg / salt_flat), so no hex can hold both and they
# only ever meet across a biome boundary. They look adjacent in `map_fauna_sprites` because that
# frame deliberately stands every species in one row, which is a property of the FRAME and not of
# the map. If either is ever regenerated, keep the saturation gap — it is doing the work here.
steppe_runner: Flat vector game icon of a steppe runner, a lean fast-running antelope of the open grassland with a deep chest, a narrow waist and long slender legs built for speed, a distinctive bulging down-turned nose, and a pair of short ringed horns rising almost straight up from its forehead, a short tufted tail, standing alert in a calm neutral pose, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in pale sandy cream and soft white that stays clearly lighter than the outline and reads clearly LIGHT overall so it stands out against mid-toned dry grassland and scrub, with a slightly warmer tan along the back, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background with the flat magenta touching the subject on every side, no rounded square, no tile, no card, no badge, no frame, no border and no background panel of any kind, the animal floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# wolf.png — Grey Wolf Pack (Predators Phase 1a — the first PREDATOR, a carnivore herd)
# The art is ONE wolf in profile (like every fauna row), not a pack — the species is "Grey Wolf
# Pack" but the marker is a single animal, the convention the whole set follows.
# GROUND-CONTRAST HAZARD (the catfish lesson, and it bites harder here): the wolf's host_biomes are
# savanna_grassland / temperate_forest / boreal_arctic / montane_highland — so it sits on PALE SNOW
# (boreal_arctic) and GREY ROCK (montane_highland). A pure grey wolf would camouflage on BOTH. The
# prompt therefore forces a strong two-tone split — a DARK grizzled grey-brown back over a PALE CREAM
# underside — so the thick charcoal outline plus the light belly carry the silhouette regardless of
# the ground beneath it. Judge it at 24px over a DARK biome (rocky highland) AND a PALE one (tundra),
# not on the magenta plate: this is exactly where the catfish's "contrast against the ground, not the
# outline" rule decides the icon. It must ALSO read as a PREDATOR at a glance — leaner and rangier
# than the grazers, erect ears, low bushy tail — so a player never mistakes the wolf marker for game.
wolf: Flat vector game icon of a grey wolf standing alert in a calm neutral pose, a lean rangy wild canine with tall erect pointed ears, a narrow tapering muzzle, a deep chest and a long bushy tail held low, shown in side profile facing right, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in grizzled dark grey-brown and slate along the back that stays clearly lighter than the outline, over a distinctly PALE cream and light grey underside on the chest, belly, throat and lower muzzle, strong two-tone contrast between the dark back and the pale underside so the shape reads over both dark rocky ground and pale snowy ground, simple two-tone shading, calm neutral facial expression, crisp clean edges, centered with even padding on a plain solid magenta background, the wolf floating with no ground shadow and no shadow beneath the feet, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# ============================================================================
# SITES  ->  assets/icons/sites/  (ui/SiteSprites.gd)
# ============================================================================
#
# Keyed by FILENAME; the comment gives the FoodIcons module the file serves (the
# SPRITE_PATHS key is the module, the file is the art). `hunt` has no file of its
# own — a hunted site reuses fauna/deer.png.
#
# Coverage is COMPLETE: 12 files, 12 prompts.


# shell.png — coastal_littoral
shell: Flat vector game icon of a small cluster of seashells, one large spiral shell and two clam shells, arranged as a compact group shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire shape, flat limited color palette in warm sand and pale coral tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# fish.png — riverine_delta
fish: Flat vector game icon of a single plump river fish seen in side profile facing right, with a broad tail fin and a visible dorsal fin, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in cool blue-grey and silver tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# grain.png — savanna_grassland
grain: Flat vector game icon of a small bundle of ripe wheat ears with heavy drooping golden heads on short stalks, tied as a compact sheaf shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in golden amber and wheat tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# chestnut.png — temperate_forest
chestnut: Flat vector game icon of three glossy chestnuts in their split spiky husk, arranged as a compact cluster shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in rich reddish-brown nuts against a pale cream husk, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# seal.png — boreal_arctic
# NOT the same image as fauna/seal.png, and neither is a stale copy of the other: this
# SITE icon is a plump seal with a ROUNDED TAIL (it stands for arctic fishing/sealing as a
# food source), while the FAUNA marker is sleeker with PAIRED REAR FLIPPERS (it stands for
# a huntable herd). Two files, two families, both legitimately named seal.png — do not
# "dedupe" them.
seal: Flat vector game icon of a plump seal resting with its head raised, shown in side profile facing right, with short front flippers and a rounded tail, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in cool slate grey and pale silver tones that stay clearly lighter than the outline, simple two-tone shading, calm neutral expression, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# tuber.png — montane_highland
tuber: Flat vector game icon of three oval root tubers with small sprouting eyes and a few thin roots, arranged as a compact cluster shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in dusty purple and warm tan tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# lotus.png — wetland_swamp
lotus: Flat vector game icon of an open water lily flower with broad rounded petals resting beside one flat lily pad, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette using a pale rose pink flower against a deep green pad, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# cactus.png — semi_arid_scrub
cactus: Flat vector game icon of a stout barrel cactus with two short upright arms and visible vertical ridges, topped with one small red bloom, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in muted sage green tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# shrimp.png — coastal_upwelling
shrimp: Flat vector game icon of a single curled shrimp seen in side profile facing right, with a segmented body, long antennae and a fanned tail, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire body, flat limited color palette in warm coral pink and orange tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# mushroom.png — mixed_woodland
mushroom: Flat vector game icon of a cluster of three round-capped mushrooms of differing heights on short thick stems, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette using warm russet brown caps above pale cream stems, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# reeds.png — FoodIcons.SITE_KEY_REEDS — the riverine reed variant
reeds: Flat vector game icon of a small stand of tall marsh reeds with narrow upright stalks and dark brown seed heads, arranged as a compact clump shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in muted olive green and warm brown tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# sprig.png — FoodIcons.SITE_KEY_DEFAULT — the unknown-module fallback
sprig: Flat vector game icon of a small leafy green sprig with a few broad simple leaves on a short stem, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline, flat limited color palette in fresh medium green tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# ============================================================================
# WONDERS  ->  assets/icons/wonders/  (ui/WonderSprites.gd)
# ============================================================================
#
# Keyed by the sim's stable `site_id` from core_sim/src/data/sites_config.json.


# great_peak.png — site_id great_peak
great_peak: Flat vector game icon of a single dramatic mountain peak with a snow-capped summit and one lower shoulder ridge, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire shape, flat limited color palette in cool slate grey and stone blue with a pale snow cap that reads clearly against the rock, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# verdant_basin.png — site_id verdant_basin
verdant_basin: Flat vector game icon of a lush green oasis basin, a small pool of blue water cradled in a bowl of green banks with two simple broad-leafed plants at its edge, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire shape, flat limited color palette using a bright blue pool against deep green banks so the water reads as the focal point, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# ============================================================================
# SETTLEMENT STAGES  ->  assets/icons/stages/  (ui/StageSprites.gd)
# ============================================================================
#
# Keyed by the sim's `settlement_stage_config.json` id — the server sends the key,
# so there is no client-side resolver. These are the ONE family whose art must read
# as a PROGRESSION, and each rung is a different KIND of thing, not just a bigger one:
# hide tent (nomadic) -> built thatched hut (camp) -> cluster of houses (village).
# Verified against the shipped PNGs. Keep that escalation when regenerating any of them.


# nomadic.png — stage id nomadic (⛺ Nomadic band)
nomadic: Flat vector game icon of a simple conical hide tent with a single pole tip poking through the top and a dark triangular door opening, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire shape, flat limited color palette in muted tan and warm grey hide tones that stay clearly lighter than the outline, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# camp.png — stage id camp (🛖 Seasonal camp)
# The middle rung: a BUILT structure rather than a tent, which is what makes the step up
# from `nomadic` legible at marker size and matches the sim's own ⛺ -> 🛖 distinction.
# (This entry was briefly a copy-paste of the `nomadic` tent prompt. The shipped
# camp.png was always the thatched hut — only the prompt was wrong.)
camp: Flat vector game icon of a single round thatched hut with a domed straw roof and a dark arched doorway, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire shape, flat limited color palette in straw gold roof above muted earth-brown walls, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# village.png — stage id village (🏘️ Village)
village: Flat vector game icon of a small cluster of three simple houses with pitched roofs at differing heights standing close together, shown from the side, drawn as a bold simplified silhouette with a thick dark charcoal outline around the entire group, flat limited color palette in muted terracotta roofs above pale clay walls, simple two-tone shading, crisp clean edges, centered with even padding on a plain solid magenta background, floating with no ground shadow, minimal graphic style that stays clearly readable when scaled down to 24 pixels, strategy game map marker icon

# ============================================================================
# CROP ROLES  ->  assets/icons/crops/  (ui/CropRoleSprites.gd)
# ============================================================================
#
# Keyed by the sim's `FloraShareInfo.role` tag verbatim (`staple` / `fodder` /
# `cash`) — the server sends the key, so there is no client-side resolver. The
# `StageSprites` degenerate case.
#
# THIS FAMILY IS NOT A MAP MARKER, AND THREE OF THE HOUSE STYLE'S CLAUSES INVERT.
# These are inline marks leading each row of the tile card's basket
# (`DetailFormat.flora_composition_lines`), drawn inside a RichTextLabel over
# `HudStyle.PANEL_SOLID`. Everything below follows from those two facts.
#
#   1. THEY RENDER AT ~13px, NOT 24-41. `%TileDetail` sets no font-size override,
#      so it takes the stock 16 through `project.godot`'s `canvas_items` stretch
#      (1920 authored). That is ROUGHLY HALF the size the entire house style above
#      is written for, and `sites/reeds.png` already blurs into a mass at ~36px.
#      Nothing with interior detail survives here. Budget ONE silhouette and AT
#      MOST ONE interior division; anything narrower than ~1/6 of the icon's width
#      is gone.
#
#   2. THE GROUND IS ONE KNOWN NEAR-BLACK PANEL, SO DROP THE THICK DARK OUTLINE.
#      `HudStyle.PANEL_SOLID` is Color(0.067, 0.102, 0.118) = #111A1E, Rec.709 luma
#      ~24 — darker than any terrain a marker sits on (the catfish note measures
#      its host grounds at 53-89). Two consequences, and the second is the one that
#      bites: contrast against the ground is nearly FREE here, so the catfish's
#      "look up the host biomes first" rule collapses to a single fixed target; but
#      a THICK DARK CHARCOAL OUTLINE — the one clause every shipped icon carries —
#      is CAMOUFLAGE against a luma-24 panel. It draws nothing and it eats the fill
#      from both sides at 13px, which is precisely the budget there is none of. The
#      FILL ITSELF carries the silhouette here. Ask for no outline at all.
#
#   3. THE TEST IS THE THREE AGAINST EACH OTHER, NOT EACH AGAINST THE GROUND. They
#      render stacked vertically, three rows apart, and the question a player asks
#      is "which of the three is this row" — never "is something there". So the set
#      is separated on TWO axes that both survive downscaling, because at 13px
#      either one alone can fail:
#         SILHOUETTE   staple TALL+NARROW · fodder LOW+WIDE · cash SQUARE+COMPACT
#         HUE          staple gold ~45deg · fodder green ~110deg · cash rose ~5deg
#      Keep both splits when regenerating any one of them. Changing a single icon's
#      colour is a change to the SET.
#
# PIPELINE DELTAS from the top of this file:
#   * Key as normal, at `--size 256`. These are pale subjects, so expect to widen
#     the ramp per the catfish note (`--lo 85 --hi 240`) and count the residual.
#   * `.import`: `process/size_limit=64` as everywhere else (headroom for HiDPI and
#     the `expand` stretch), but MIPMAPS ARE LIVE HERE — `mipmaps/generate=true` is
#     inert in all four marker families because `MapView` pins
#     `TEXTURE_FILTER_NEAREST` on itself, and a RichTextLabel does NOT inherit that.
#     `project.godot` sets no `default_texture_filter`, so this family draws through
#     Godot's Linear default and genuinely uses the mip chain. Do not copy a fauna
#     sidecar and assume the flag is decorative.
#   * JUDGE AT ~13px OVER #111A1E, and judge all three TOGETHER — a contact sheet of
#     the three stacked as they render in the basket, not one icon at a time. The
#     shipped frames are ui_preview's `tile_food_layers` (all three roles) and
#     `tile_food_layers_unstated` (the blank-slot row).
#
# WHAT EACH MARK HAS TO SAY (from `FoodIcons.CROP_ROLE_ICONS`, and the reason the
# borrowed emoji are being retired): staple = food for PEOPLE; fodder = feed for
# PENNED ANIMALS, not food for people; cash = goods to TRADE. The retired glyphs
# were the wheat ear / the cow / the exchange arrow, and the last two both still
# mean something else elsewhere in the HUD (a penned herd on the work board; trade
# goods on every yield readout) — so DO NOT DRAW AN ANIMAL FOR `fodder` AND DO NOT
# DRAW AN ARROW, COIN OR SCALE FOR `cash`. Re-introducing either subject
# re-introduces the collision this art exists to remove. Each mark names the
# PRODUCT, not the account it pays into.
#
# THESE THREE ARE THE FALLBACK TIER OF THEIR SLOT, NOT THE ONLY TENANT OF IT.
# A basket row prefers its plant's own `flora/` icon and falls back to the role
# mark here, so as flora art lands each of these renders on FEWER rows — the
# `fodder` bale, for one, is already the whole answer for `hay_grass` and that
# species is deliberately never given flora art. Two consequences: the set is
# still judged as three against each other (a row shows one mark, and which of
# the three it is remains the question), and a `flora/` prompt must never draw
# one of these three subjects, or a row would say the same thing twice under
# two different names.


# AS SHIPPED — measured at 13px over #111A1E, which is the only size that decides
# anything here. All three keyed at `--lo 85 --hi 240`:
#
#   mark     silhouette (w x h)   ink    luma   residual bg
#   staple   3 x 8                16.7%  191    1
#   fodder   11 x 5               29.9%  158    12
#   cash     10 x 11              55.5%  134    0
#
# The SILHOUETTE column is the whole design: a thin vertical, a wide horizontal and
# a solid block are three different shapes before they are three different colours,
# which is what makes the set survive at a size where interior detail does not.
# Every luma is 5-8x the panel's 24, so contrast never came under pressure — the
# "contrast is nearly free on this panel" claim above is now measured, not assumed.
#
# TWO THINGS THAT DISSOLVE, both expected and both fine: fodder's binding band and
# cash's cord are gone below ~26px, so those two read as a plain bar and a plain
# block in play. They were asked for as the ONE permitted interior division and
# they earn their keep at 26px+; do not "fix" this by thickening them, which would
# split each mark into two shapes at row size. It is the `reeds` trade again.
#
# The cash render came back on a NAVY background rather than magenta and keyed
# cleanly anyway (0 residual) — `icon_key.py` samples the corners, so any flat
# uniform field works. Its fold notches are that background showing THROUGH, so
# they key to transparent and the panel shows through them, which is why they cost
# nothing. Worth knowing before "correcting" the background of a future render.


# staple.png — role `staple` (food for people; retires the wheat-ear emoji)
# TALL + NARROW, warm gold. Deliberately NOT `sites/grain.png`, which is a TIED
# SHEAF of several stalks shown from the side and dissolves into a mass at this
# size — this is ONE ear, which is what leaves enough width per grain to read.
staple: Flat vector game icon of a single upright ear of ripe wheat, one fat seed head of plump overlapping grains on a very short stubby stalk with no leaves, tall and narrow in overall shape, drawn as one bold flat silhouette with NO outline of any kind, filled in a single bright warm golden amber tone with at most one slightly darker gold division separating the grains, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# fodder.png — role `fodder` (feed for penned animals; retires the cow emoji)
# LOW + WIDE, fresh green. NO ANIMAL — the retired cow is `POLICY_CORRAL`'s penned-
# livestock mark and still means that on the work board and the map. A bound bale of
# cut green forage says "feed" by the product instead, and its squat horizontal
# block is the strongest silhouette of the three at 13px.
fodder: Flat vector game icon of a bound bale of cut green forage, a squat low wide rectangular block of stacked hay with slightly ragged ends and one simple horizontal binding band across its middle, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single fresh medium green tone with at most one slightly darker green band for the binding, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no individual straws, no fine texture, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# cash.png — role `cash` (goods to trade; retires the exchange arrow)
# SQUARE + COMPACT, dusty rose. NO ARROW, COIN OR SCALE — the exchange arrow is
# `FoodIcons.TRADE_GOODS_GLYPH` on every yield readout in the game, and the coin /
# money-bag / scales emoji are separately measured and rejected at these sizes. The
# cash crops here are fibre (cotton, flax), so a wrapped bolt of dyed cloth names
# the PRODUCT rather than the account. The rose hue is the far side of the wheel
# from both siblings.
cash: Flat vector game icon of a compact upright bolt of folded dyed cloth, a roughly square blocky bundle of neatly folded fabric with one simple diagonal binding cord across its face, drawn as one bold flat silhouette with NO outline of any kind, filled in a single warm dusty rose terracotta tone with at most one slightly darker rose line for the cord, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fabric texture, no fine details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ============================================================================
# FLORA SPECIES  ->  assets/icons/flora/  (ui/FloraSprites.gd)
# ============================================================================
#
# Keyed by the sim's `FloraShareInfo.species` verbatim, and THE FILENAME IS THE
# KEY: `wild_emmer` reads `assets/icons/flora/wild_emmer.png`. There is no table
# in the client to edit — dropping a keyed PNG into that directory wires it.
# The 33 keys are `species` in `core_sim/src/data/flora_config.json`.
#
# THIS FAMILY TAKES THE `crops/` SUB-STYLE, NOT THE HOUSE STYLE ABOVE. These
# icons render in the SAME `RichTextLabel` row as the crop-role marks, at the
# same ~13px, over the same `HudStyle.PANEL_SOLID` (#111A1E). So every clause
# the crops section inverts is inverted here too, and for the same measured
# reasons: NO OUTLINE (a thick dark charcoal outline is camouflage against a
# luma-24 panel and eats the fill from both sides at 13px), ONE silhouette and
# AT MOST ONE interior division, and the FILL itself carries the shape. Read
# that section before generating one; do not start from the house style.
#
# FILL LUMA IS TWO TIERS AGAINST THE PANEL'S 24, and both are NUMBERS rather than
# adjectives, because an adjective is what failed:
#
#   TARGET BAND  ~130-190  where the shipped `crops/` marks measure (134-191, the
#                          table above, 5-8x the panel) and where most of this set
#                          landed. A species whose natural colour permits it
#                          belongs here, and the majority of them are here.
#   HARD FLOOR   ~55       a little over twice the panel, below which a fill stops
#                          reading as a shape at all. 55-130 is the legitimate home
#                          of a species whose true colour IS dark — the deep
#                          purples, the dark fruits, the browns.
#
# THE FLOOR WAS A FLAT 130 UNTIL ALL 32 WERE GENERATED AND MEASURED, and seven of
# them came in under it while reading perfectly well in the basket — so the number
# was wrong, not the art. 130 came from `crops/`, where the mark is the row's ONLY
# graphic; a flora icon sits beside the plant's NAME in text and does not have to
# carry the row alone. The seven, lowest first:
#
#   berry_scrub 59 · wild_fig 72 · pine_nut 76 · wild_orchard 78 · grapevine 79 ·
#   chestnut 97 · cattail 115
#
# `berry_scrub` at 59 is the darkest thing the shipped set accepts and is the
# practical bottom of the low band. (A floor of 70 was proposed and would have
# failed shipped art on the spot — the floor has to sit BELOW the darkest icon we
# accept, which is why it is 55 and why the table is quoted here: a rule with the
# real numbers beside it is checkable where an adjective is not.)
#
# WHAT A DIM FILL COSTS IS LEGIBILITY, AND THE SILHOUETTE THEN HAS TO PAY FOR IT,
# so a species may sit in the low band ONLY IF its outline is strong and
# distinctive. All seven qualify on exactly that ground: a spiked star, a
# triangular bunch of circles, a stepped cone, a smooth teardrop, a tight cluster
# of four circles, a blunt cylinder. `kelp` is the case that produced this rule and
# its ~65 sits INSIDE the permitted band, which is not a contradiction: it was
# rejected because a long thin ribbon blade at 65 has no silhouette left to carry
# it, where berry_scrub at 59 reads by shape. Same number, different outline,
# different answer. WHERE A SPECIES' COLOUR IS GENUINELY DARK AND ITS SILHOUETTE IS
# WEAK, THE ICON TAKES A LIGHTER MEMBER OF THAT HUE FAMILY, not the true one: a
# plant that reads as a smudge has failed regardless of how accurate its colour is.
#
# NONE OF THIS IS LICENCE TO DRIFT DARK. Most icons belong in the target band; the
# low band is for species whose colour is genuinely dark, not a relaxed floor
# everyone may use. And it does not soften the TONE-WORD rule, which is a separate
# failure from merely landing dim and is what actually killed kelp: kelp asked for
# "a single dark olive green", the generator obeyed exactly, and the same prompt's
# boilerplate was still asking for a shape "reading clearly LIGHT and
# high-contrast". A tone word that names darkness OUTRANKS that boilerplate in
# practice, so do not write one — no fill clause in this section may say "dark",
# and `grep "^[a-z_]*:.*single dark" icon_prompts.txt` must keep returning nothing.
# (The pattern is anchored to a PROMPT line for a reason: an unanchored grep for
# the phrase matches this rule stating it, and comes back looking like a breach.)
#
# A SECOND HOST, at the same size class, so ONE piece of art serves both: the
# crop picker's rows are `Button`s and take the art on their `icon` property at
# `HudFloraVocab.FLORA_CROP_ICON_MAX_WIDTH` (16) inside a 22px row.
#
# A SPECIES ICON REPLACES THE ROW'S CROP-ROLE MARK — they never render together
# (`DetailFormat.flora_composition_lines`: species art -> role art -> role emoji
# -> blank slot). Two consequences, and the second is the one that wastes a
# whole art family if it is missed:
#
#   * A row with no art here is NOT bare — it keeps its role mark. So a species
#     whose role mark already names it needs no art at all (see `hay_grass`).
#   * THE ART MUST CARRY THE PLANT, NEVER THE ROLE. Do not draw a wheat ear for
#     every staple and do not draw a bolt of cloth for the cash crops: those are
#     `crops/staple.png` and `crops/cash.png`, and repeating them here spends a
#     new icon on re-saying exactly what the mark it displaced already said.
#
# THE TEST IS THE BASKET, NOT THE ROSTER — and this is what makes 32 legible
# icons at 13px possible at all. A tile realizes only `realized_species_min` 2
# to `realized_species_max` 4 species (`flora_config.json`), so a player never
# sees more than four of these at once and an icon only has to be told apart
# from the plants that can stand BESIDE it. Co-hosting is broad, though — 19
# species share a biome with wild emmer — so the crops family's two-axis rule
# is applied per CLUSTER below: SILHOUETTE first, HUE second, and every pair
# that can co-host differs on at least one. Keep both splits when regenerating
# any one icon; changing a single icon's shape or colour is a change to its
# CLUSTER, not to that icon.
#
# `hay_grass` IS DELIBERATELY ABSENT, and that is the right answer rather than
# a gap. It is the roster's only `fodder` species, so the fodder role mark — a
# bound bale of cut forage — already names it exactly and uniquely; and a
# standing-grass silhouette would collide with the four other grass spikes it
# co-hosts with (wild emmer, seed grasses, wild rice, cattail), which is the one
# cluster with no room to spare. It keeps its role mark. 32 prompts, 33 species.
#
# SEVEN OF THESE SUBJECTS ARE ALREADY DRAWN IN `sites/`, AND NONE OF THAT ART
# CAN BE REUSED HERE — but all of it is the REFERENCE to draw from, so that one
# plant does not end up with two unrelated looks in one game:
#
#   sites/chestnut.png  <- flora chestnut       (the same word, even)
#   sites/grain.png     <- wild_emmer, seed_grasses
#   sites/reeds.png     <- reed_and_root, cattail
#   sites/tuber.png     <- wild_tubers
#   sites/mushroom.png  <- cave_fungi
#   sites/fish.png      <- river_fish
#   sites/shell.png     <- shellfish_beds
#
# Three reasons the file cannot simply be pointed at, and the third is the one
# that would still bite after a rescale:
#
#   1. THEY WEAR THE HOUSE STYLE'S THICK DARK CHARCOAL OUTLINE, which is
#      camouflage against a luma-24 panel and eats the fill from both sides at
#      13px — the measured reason the `crops/` sub-style drops it.
#   2. THEY ARE MEASURED TO FAIL AT THIS SIZE, in this file, already:
#      `sites/reeds.png` "blurs into a mass at ~36px", and `crops/staple.png`
#      exists as ONE ear precisely because `sites/grain.png` is a tied sheaf of
#      several stalks that "dissolves into a mass at this size".
#   3. THEY ANSWER A DIFFERENT QUESTION. A site icon is keyed by FOOD MODULE —
#      one of ten buckets — and is deliberately generic: `chestnut.png` stands
#      for the whole `temperate_forest` module, not for the `chestnut` species.
#      Wiring it to the species would make one picture mean the bucket on the
#      map and one plant in the basket.
#
# So: redraw them in this section's style, keeping the existing icon's SUBJECT
# and PALETTE where one exists, and simplifying its silhouette to survive 13px.
#
# PIPELINE DELTAS from the top of this file — the `crops/` set's, unchanged:
#   * Key at `--size 256`. Many of these are pale subjects, so expect the
#     catfish note's wider ramp (`--lo 85 --hi 240`) and COUNT THE RESIDUAL.
#   * `.import`: `process/size_limit=64`, and MIPMAPS ARE LIVE (`mipmaps/
#     generate=true`) — a RichTextLabel does not inherit MapView's pinned
#     `TEXTURE_FILTER_NEAREST` and genuinely uses the mip chain.
#   * Carry the no-tile counter-clause in every prompt below verbatim, and read
#     `icon_key.py`'s printed COVERAGE figure before anything else: a bare plant
#     is ~15-25% of the canvas, and a figure far above that means a background
#     tile survived the key.
#   * JUDGE AT ~13px OVER #111A1E, AND JUDGE THE CLUSTER TOGETHER — a contact
#     sheet of the cluster stacked as basket rows, never one icon at a time.
#     The question a player asks is "which of these rows is which plant".


# ----------------------------------------------------------------------------
# CLUSTER A — GRASS SPIKES & HEADS (the temperate/river staples, and the
# tightest cluster in the set: four of these co-host almost everywhere)
#   wild_emmer    STOUT LOBED UPRIGHT SPIKE   ripe gold
#   seed_grasses  WIDE AIRY SPRAY             pale straw
#   wild_rice     ARCHED DROOPING SPRAY       fresh green
#   cattail       BLUNT CYLINDER ON A STEM    warm mid-brown
#   sunflower     ROUND RAYED DISC            gold, dark centre
# ----------------------------------------------------------------------------

# wild_emmer.png — Wild Emmer (staple)
# REVISED, not authored blind: the first generation reduced to a ~2px-wide vertical
# stripe at row size (measured). The cause is ASPECT, not tone — `icon_key.py` trims
# to the subject's bounding box and fits it into a square PRESERVING ASPECT, so the
# subject's own proportions decide how much width survives, and a very tall very
# narrow ear lands as a hairline. Long awns only make it worse: they add height and
# no width. Hence a stouter head, stubby awns, and a NUMBER for the proportion
# instead of an adjective — the luma-floor paragraph makes the number-not-adjective
# argument for colour, and this is the same lesson applied to GEOMETRY.
# WHAT THE STOUTNESS MUST NOT COST — cluster A's separation:
#   vs seed_grasses  emmer stays ONE solid upright spike, seed grasses is a fan of
#                    several thin stems. Fattening the ear does not threaten this.
#   vs cattail       the real risk, cattail being a blunt solid cylinder. What must
#                    survive is that emmer's outline is LOBED by its grain rows
#                    where cattail's is SMOOTH, and that emmer is gold where cattail
#                    is brown. Never let "fatter" become "smooth-sided".
wild_emmer: Flat vector game icon of a single upright ear of wild emmer wheat, one short fat seed head of plump grains stacked in paired rows so its two side edges are clearly lobed and bumpy rather than smooth, three or four short stubby bristles at its tip and no long awns, standing on a very short bare stalk, the overall shape upright but stout, no more than about two and a half times taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single ripe golden amber tone with at most one slightly darker gold division down its centre, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# seed_grasses.png — Seed Grasses (staple)
# WIDE and AIRY where emmer is one solid spike — the silhouette split is the
# whole distinction, since both are pale straw-gold heads.
seed_grasses: Flat vector game icon of a loose open spray of wild grass seedheads, five or six thin stems fanning outward and upward from one point with a small tuft of seed at each tip, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale straw tone with no interior division at all, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# wild_rice.png — Wild Rice (staple)
# The ARCH is what tells it from emmer and seed grasses; the green seals it.
wild_rice: Flat vector game icon of a single stem of wild rice arching over to one side with a heavy drooping one-sided head of grains hanging from its curved tip, a clear sideways C-shaped arc in overall silhouette, drawn as one bold flat silhouette with NO outline of any kind, filled in a single fresh medium green tone with at most one slightly darker green division separating the head from the stem, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# cattail.png — Cattail & Bulrush (staple)
# The one BLUNT SOLID shape in a cluster of feathery ones, and the only brown —
# it should read instantly even at row size. (It reads as a DARK cattail beside
# gold and straw because of hue, not because the fill is dark: the fill is a warm
# MID-brown and must stay one — see the luma floor in the preamble.)
cattail: Flat vector game icon of a single cattail bulrush, one fat smooth blunt-ended brown cylinder standing upright on a straight thin stem with one narrow blade leaf angling off the stem, drawn as one bold flat silhouette with NO outline of any kind, filled in a single warm mid-brown tone with at most one slightly darker brown division where the cylinder meets the stem, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# sunflower.png — Sunflower & Seedheads (staple)
# A perfect disc — the only fully round silhouette in this cluster.
sunflower: Flat vector game icon of a single sunflower head seen face on, a round dark seed centre ringed by one row of short broad triangular petals, roughly circular in overall shape with no stem and no leaves, drawn as one bold flat silhouette with NO outline of any kind, filled in a single bright golden yellow with one clearly darker brown circle for the seed centre, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER B — NUTS & MAST (all four co-host across the temperate woodland, so
# all four are separated on SILHOUETTE alone — they are all brown)
#   hazel      ROUND NUT IN A FRINGED HUSK   warm hazel
#   oak_mast   EGG UNDER A SCALED CAP        mid brown
#   chestnut   SPIKED SPHERE, SPLIT          russet
#   pine_nut   SCALED TEARDROP CONE          deep brown
# ----------------------------------------------------------------------------

# hazel.png — Hazel (staple)
hazel: Flat vector game icon of a single hazelnut sitting in its ragged frilled husk, one smooth round nut with a torn fringe of husk leaves flaring around its base, roughly round with a spiky lower edge, drawn as one bold flat silhouette with NO outline of any kind, filled in a single warm mid-tan tone with at most one slightly darker tan division between the nut and its husk, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# oak_mast.png — Oak Mast (staple)
oak_mast: Flat vector game icon of a single acorn, one smooth oval nut hanging point-down beneath a thick textured cap that covers its top third, taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single light warm brown with one clearly darker brown cap, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# chestnut.png — Sweet Chestnut (staple)
# The SPIKES are the silhouette. Keep them few and fat — four or five, not a
# halo of fine ones, which dissolve to a fuzzy circle at 13px.
chestnut: Flat vector game icon of a sweet chestnut burr split open, a round shell with five or six short fat triangular spikes around its edge and one smooth glossy nut showing in the opening, roughly round and clearly spiked in overall silhouette, drawn as one bold flat silhouette with NO outline of any kind, filled in a single russet red-brown tone with one clearly lighter oval for the nut, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# pine_nut.png — Pine Nut (staple)
# The cone's scales are ONE interior division read as a stepped edge, not a
# grid of individual scales — at 13px a scale grid is grey mush.
pine_nut: Flat vector game icon of a single pine cone standing upright, a tapering teardrop shape whose two side edges are stepped into four or five broad scale notches, taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single deep warm brown with no interior lines beyond the stepped edges themselves, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER C — BERRIES (these two co-host; MANY-vs-ONE and purple-vs-amber)
#   berry_scrub  CLUSTER OF ROUND BERRIES   deep purple
#   cloudberry   ONE KNOBBLY AGGREGATE      amber orange
# ----------------------------------------------------------------------------

# berry_scrub.png — Berry Scrub (staple)
berry_scrub: Flat vector game icon of a small tight cluster of four round berries hanging together from one short twig with a single small leaf, the berries clearly separate circles touching each other, drawn as one bold flat silhouette with NO outline of any kind, filled in a single deep plum purple with at most one slightly darker purple division between the berries and the twig, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# cloudberry.png — Cloudberry & Crowberry (staple)
cloudberry: Flat vector game icon of a single cloudberry, one plump raspberry-like berry built from six or seven fat rounded lobes so its outline is clearly bumpy, sitting on a short stem with two small pointed leaves beneath it, drawn as one bold flat silhouette with NO outline of any kind, filled in a single warm amber orange with at most one slightly darker amber division between the berry and its leaves, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER D — TREE FRUIT & VINE (grapevine co-hosts all three of the others)
#   wild_orchard  ONE ROUND FRUIT + LEAF        red
#   wild_fig      ONE TEARDROP FRUIT            deep violet
#   date_palm     FROND ABOVE A HANGING BUNCH   amber + light green frond
#   grapevine     TRIANGULAR BUNCH + LEAF       deep purple
# NOTE — wild_fig and grapevine are the tightest pair here (both dark purple).
# They are held apart by SILHOUETTE alone: one smooth teardrop against a
# triangular mass of small circles. Do not narrow that gap by making the fig
# lumpy or the bunch smooth.
# ----------------------------------------------------------------------------

# wild_orchard.png — Wild Orchard (staple)
wild_orchard: Flat vector game icon of a single round orchard apple on a short stem with one broad pointed leaf angling off to the side, a clean circle with a small notch at the top, drawn as one bold flat silhouette with NO outline of any kind, filled in a single bright warm red with one clearly darker green leaf, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# wild_fig.png — Wild Fig (staple)
wild_fig: Flat vector game icon of a single fig, one smooth teardrop fruit hanging point-up from a very short stalk, broad and rounded at the bottom and tapering to the stalk, with no leaves at all, drawn as one bold flat silhouette with NO outline of any kind, filled in a single deep violet tone with at most one slightly darker violet division at the stalk, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# date_palm.png — Date Palm (staple)
# The FROND ON TOP is what stops it reading as a second grape bunch.
# REVISED, not authored blind: the first generation keyed the frond NEAR-BLACK
# (measured), so at 13px it vanished into the panel and left a bare hanging mass of
# small ovals — which is grapevine. The clause that caused it was "one clearly
# darker green frond", so the two-element split is no longer subject-plus-a-darker-
# mark: it is amber fruit against a LIGHT green frond, held apart by HUE with both
# elements up in the target band, not merely over the floor. Weight is the second
# half of the fix —
# even a light frond is marginal as a thin arc at this size, so the blade is asked
# for BROAD and THICK. It stays ONE smooth blade rather than a fan of serrated
# leaflets: that is the chestnut-spike rule (few and fat, never a halo of fine
# ones) applied here.
date_palm: Flat vector game icon of a hanging bunch of dates beneath one arching palm frond, the frond a single broad thick smooth-edged curved blade spanning the whole top, solid and noticeably heavier than a thin stroke, with no separate leaflets and no serrations, and the dates six or seven small ovals gathered in a mass below it, drawn as one bold flat silhouette with NO outline of any kind, filled in just two flat tones, warm amber for the dates and a light fresh green for the frond, the two told apart by HUE rather than by lightness and both plainly on the light side so each reads bright at row size, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# grapevine.png — Wild Grapevine (cash)
grapevine: Flat vector game icon of a hanging bunch of grapes, seven or eight small circles packed into a clear downward-pointing triangle, with one small lobed vine leaf attached at its top right, drawn as one bold flat silhouette with NO outline of any kind, filled in a single deep purple with one clearly darker green leaf, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER E — THE CASH CROPS (four of the five; grapevine is in cluster D)
#   cotton    WHITE PUFF IN A DARK STAR BRACT   white
#   flax      THIN STEM + FLAT STAR FLOWER      cornflower blue
#   tobacco   ONE LARGE DROOPING LEAF           olive
#   tea       TWO SMALL LEAVES AND A BUD        bright green
# NOTE — tobacco and tea are the tightest pair (both are green leaves). They are
# held apart on BOTH axes at once: one big drooping blade against two small
# upright ones, and olive against bright green. Regenerating either without the
# other on the contact sheet is how they converge.
# NONE of these may be drawn as a bolt of cloth — that is `crops/cash.png`, the
# very mark this art replaces.
# ----------------------------------------------------------------------------

# cotton.png — Cotton Fields (cash)
cotton: Flat vector game icon of a single open cotton boll, a soft round white puff of fibre sitting in a four-pointed dark star-shaped bract that opens beneath and around it, drawn as one bold flat silhouette with NO outline of any kind, filled in a single near-white cream with one clearly darker warm brown bract, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# flax.png — Flax Fields (cash)
# The ONE BLUE icon in the whole set — the hue is doing most of the work here,
# so do not warm it toward teal.
flax: Flat vector game icon of a single flax flower on a slender upright stem, one flat five-petalled star-shaped bloom seen face on at the top of a thin bare stalk with two narrow leaves low on the stem, tall and narrow overall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single clear cornflower blue with at most one slightly darker blue centre to the flower, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# tobacco.png — Tobacco Fields (cash)
tobacco: Flat vector game icon of one large broad tobacco leaf hanging from a short stem, a single wide oval blade with a gently wavy edge and a pronounced droop to one side, filling most of the frame on its own, drawn as one bold flat silhouette with NO outline of any kind, filled in a single dull olive green with one clearly darker olive line for the central vein, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# tea.png — Tea Gardens (cash)
tea: Flat vector game icon of a tea shoot, two small pointed leaves angling upward and outward from one short stem with a single narrow unopened bud standing between them, small and clearly three-pronged in overall silhouette, drawn as one bold flat silhouette with NO outline of any kind, filled in a single bright fresh green with at most one slightly lighter green bud, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER F — ROOTS, REEDS & GOURDS
#   wild_tubers    LOW LOBED CLUSTER + ROOT TAILS   earth tan
#   reed_and_root  TALL STEM WITH A BASE KNOT       reed green
#   desert_gourd   ROUND STRIPED MELON, LYING       pale yellow-green
# ----------------------------------------------------------------------------

# wild_tubers.png — Wild Tubers (staple)
wild_tubers: Flat vector game icon of a small clump of three rounded root tubers touching each other, each an irregular oval with one thin wispy root tail trailing from its end, clearly wider than it is tall and sitting low in the frame, drawn as one bold flat silhouette with NO outline of any kind, filled in a single warm earth tan with at most one slightly darker tan division between the tubers, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# reed_and_root.png — Reeds & Water Roots (staple)
# TALL where the tubers are low; the knuckled base is what says "and root".
reed_and_root: Flat vector game icon of a single tall straight reed, one thin vertical stem with two narrow blade leaves sheathing it partway up and a thick knuckled horizontal rhizome at its base, clearly taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single muted reed green with one clearly darker tan rhizome at the base, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# desert_gourd.png — Desert Gourd (staple)
desert_gourd: Flat vector game icon of a single round desert gourd lying on its side, a plump circle slightly wider than tall with one short curled stem at its top and two broad vertical stripes across its face, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale yellow-green with one clearly darker green stripe, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER G — PODS (these two co-host; CURVED-OPEN-GREEN vs STRAIGHT-HUNG-TAN)
#   wild_pulses  CURVED SPLIT POD, PEAS SHOWING   fresh green
#   mesquite     LONG STRAIGHT HANGING POD        dry tan
# ----------------------------------------------------------------------------

# wild_pulses.png — Wild Pulses (staple)
wild_pulses: Flat vector game icon of a single pea pod split open along its length, a gently curved crescent shell lying open with three round peas resting in a row inside it, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single fresh medium green with one clearly lighter green for the three peas, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# mesquite.png — Mesquite & Acacia (staple)
mesquite: Flat vector game icon of a long straight mesquite seed pod hanging down from a short bare twig, one narrow vertical bean pod with four or five gentle bulges along its length showing the seeds inside, clearly taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single dry pale tan with at most one slightly darker tan division at the twig, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER H — COAST & WATER
#   kelp             RIBBON BLADE + FLOAT BULB   mid sea-green
#   sea_kale         BROAD CRINKLED BLADE        blue-green
#   shellfish_beds   FAN-RIBBED BIVALVE SHELL    pale grey
#   river_fish       SIDE-PROFILE FISH           steel blue
# river_fish co-hosts NOTHING — it is the navigable channel's own basket member
# (`_comment_river_fish` in flora_config.json), so it never has to be told apart
# from anything. It still needs to read as a fish at 13px.
# ----------------------------------------------------------------------------

# kelp.png — Kelp Beds (staple)
kelp: Flat vector game icon of a single kelp frond, one long narrow ribbon blade with a gentle S-curve and a wavy edge, rising from a single round float bulb at its base, clearly taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single mid sea-green tone, cool and clearly oceanic but plainly on the LIGHT side so the blade reads bright at row size, with at most one slightly darker division where the blade meets the bulb, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# sea_kale.png — Sea Kale & Samphire (staple)
sea_kale: Flat vector game icon of a single broad sea kale leaf, one wide rounded blade with a strongly crinkled ruffled edge and a short thick stalk, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale blue-green with one clearly darker line for the central vein, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# shellfish_beds.png — Shellfish Beds (staple)
shellfish_beds: Flat vector game icon of a single scallop shell seen face on, a fan-shaped shell with a straight hinge along the bottom, a rounded top edge and four broad radiating ribs, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale warm grey with one slightly darker grey for the ribs, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# river_fish.png — River Fish & Mussels (staple)
# Side profile facing RIGHT, matching every bundled fauna sprite in this file.
river_fish: Flat vector game icon of a single fish in side profile facing right, a simple rounded body tapering to a forked tail with one small triangular dorsal fin and one round eye, clearly wider than it is tall, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale steel blue with one clearly darker blue eye, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no scales, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ----------------------------------------------------------------------------
# CLUSTER I — COLD & BARREN GROUND (all four co-host across the polar, alpine
# and cavern biomes, and they are the palest subjects in the set — expect to
# widen the key ramp furthest here)
#   arctic_greens  LOW ROUND ROSETTE            bright green
#   rock_tripe     FLAT IRREGULAR CRUST         grey-green
#   alpine_herbs   THIN STEM + SMALL FLOWERHEAD magenta on green
#   cave_fungi     DOMED CAP ON A STALK         pale cream
# ----------------------------------------------------------------------------

# arctic_greens.png — Arctic Greens (staple)
arctic_greens: Flat vector game icon of a low rosette of arctic greens seen from above, five small round leaves overlapping in a tight circle around one centre, wider than it is tall and sitting low with no stem showing, drawn as one bold flat silhouette with NO outline of any kind, filled in a single bright fresh green with at most one slightly darker green division between the leaves, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# rock_tripe.png — Rock Tripe & Lichen (staple)
# The famine floor of the roster, and the only IRREGULAR silhouette in the set —
# every other icon is a recognisable regular shape, which is the point.
rock_tripe: Flat vector game icon of a flat irregular patch of rock lichen, one ragged lobed disc with a torn uneven edge and a slight curl at one side, clearly wider than it is tall and clinging flat, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale sage grey-green with at most one slightly darker lobe, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# alpine_herbs.png — Alpine Herbs & Roseroot (staple)
alpine_herbs: Flat vector game icon of a single alpine herb stem, one thin upright stalk with three pairs of small narrow leaves down its length and a small rounded cluster of flowers at its tip, clearly taller than it is wide, drawn as one bold flat silhouette with NO outline of any kind, filled in a single muted sage green with one clearly brighter magenta pink flower head, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon

# cave_fungi.png — Cave Fungi (staple)
cave_fungi: Flat vector game icon of a single mushroom seen from the side, one smooth wide domed cap sitting on a short thick straight stalk, the cap clearly wider than the stalk and slightly overhanging it, drawn as one bold flat silhouette with NO outline of any kind, filled in a single pale cream with one clearly darker division where the cap meets the stalk, the shape reading clearly LIGHT and high-contrast so it stands out sharply against a near-black dark blue-grey panel, no gradients, no gills, no fine texture, no small details, crisp clean edges, the subject drawn DIRECTLY on the magenta with nothing behind it — no rounded square, no tile, no card, no badge, no button, no frame, no border and no background shape of any kind, the flat magenta touching the subject on every side, centered with even padding on a plain solid magenta background, floating with no ground shadow, extremely minimal graphic style that stays clearly readable when scaled down to 13 pixels, user interface row icon


# ============================================================================
# HUD / ACTIVITY MARKS  ->  assets/icons/hud/   (NO LOOKUP SCRIPT YET)
# ============================================================================
#
# ⚠ ALL FOUR MARKS EXIST; NONE OF THE WIRING DOES. `assets/icons/hud/{cairn,forage,hunt,scout}.png`
# are keyed, imported and committed — and NOTHING
# LOADS ANY OF THEM. There is no `UiSprites.gd` and no call site. That is deliberate: the
# cairn's wiring is issue #581's own work (a `Button.icon` on the action-bar launcher, a
# `TextureRect` swapped for a `Label` on the turn orb's row) and the other three are #249's,
# which is still blocked on the ladder-set question below. Do not read this block as a seventh
# art family: the six listed at the top of this file are the six with a lookup script.
#
# JUDGE ONE OF THESE AT 13px OR NOT AT ALL, and judge it by INK COVERAGE. That single number
# predicted every outcome in this batch: forage 32% read instantly; hunt survives at 6.8% only
# because its ink is ONE continuous bar; scout v1 at 7.7-10% did not survive at all. The
# cheapest test there is: key it, resize to 13x13, and composite it on #111A1E.
# THE DIRECTORY IS `hud/`, AND ALL FOUR SHARE IT. The cairn briefly had an `ui/` of its own —
# #581 says only "beside `stages/` and `fauna/`" — and it was folded in here rather than left
# as a second one-file directory. One home for every mark drawn against the dark HUD panel is
# also what makes the sub-style below a property of the DIRECTORY rather than a coincidence:
# everything in `hud/` takes it, and a mark that would not is a mark that belongs elsewhere.
#
# THEY TAKE THE `crops/` SUB-STYLE, NOT THE HOUSE STYLE, and that is the one thing to get
# right before generating any of them. Every subject here is an inline mark inside a HUD
# row or on a small button, drawn over `HudStyle.PANEL_SOLID` = #111A1E (luma 24) at
# 13-24px — the `crops/` section's exact situation, so its three inversions apply verbatim:
#
#   * NO OUTLINE AT ALL. A thick dark charcoal outline is the one clause every map marker
#     carries and it is CAMOUFLAGE against a luma-24 panel — it draws nothing and eats the
#     fill from both sides at a size that has no pixels to spare. The FILL carries the
#     silhouette here.
#   * NO SIDE PROFILE. The "facing right" clause exists because markers sit in map rows
#     beside each other; a button icon is symmetrical and front-on.
#   * CONTRAST AGAINST THE GROUND IS FREE, so the catfish's "look up the host biomes"
#     rule collapses to one fixed target: pale fill on a near-black panel.
#
# Key at `--size 256`, expect the pale-subject ramp (`--lo 85 --hi 240`), and note that
# MIPMAPS ARE LIVE for this family for the same reason they are for `crops/`: a
# RichTextLabel does not inherit `MapView`'s pinned `TEXTURE_FILTER_NEAREST`.


# cairn.png — issue #581, the knowledge screen's launcher.
# Replaces `HudKnowledgeVocab.LAUNCH_GLYPH` (a text ▲) on TWO surfaces that need different
# mechanisms — a `Button.icon` on the action-bar launcher, a `TextureRect` swapped for a
# `Label` on the turn orb's `knowledge_learned` row. The art is one file either way.
# It must read at ~16px on the collapsed rail, which is the smallest thing asked of any
# art in this repo, so the shape budget is ONE silhouette and at most one interior line.
# STATUS: ART SHIPPED at `assets/icons/hud/cairn.png`, NOT WIRED — see the section warning.
# Keyed at the pale-subject ramp (`--size 256 --lo 85 --hi 240`); coverage 20.4%, magenta
# residual **0**, the cleanest key in the whole drop.
# MEASURED — coverage 20.6%, fills at luma 200 and 226 against the panel's 24, i.e. a ground
# gap of 177-202. Contrast here was never going to be the problem (the `crops/` note: on a
# luma-24 panel it is nearly free); the two things worth measuring were that no tile survived
# and that the stack is taller than it is wide, and both hold at bbox 486x720.
# THE CHOSEN RENDER — of two near-identical candidates, the one with the STEPPIER profile won,
# and the reason is what this icon is replacing. The stone-to-stone contrast is only ~26 luma,
# so at 16px the interior divisions disappear and what is left is the OUTLINE OF A TAPERED
# TOWER. The rejected candidate tapered smoothly and evenly, which at that size resolves to a
# plain pale cone — and a cone is a TRIANGLE, which is precisely the `▲` placeholder this art
# exists to replace, so it would have bought nothing. The chosen one steps its width in visible
# jumps over a wide flat base stone, so it still reads as stacked objects once the divisions
# are gone. Judge any future candidate on the 16px silhouette alone, never on the stone detail.
cairn: Flat vector interface mark of a cairn, a small freestanding stack of five or six flat rounded fieldstones piled into a narrow tapering tower with the largest stone at the base and the smallest on top, seen straight on from the front in a symmetrical upright pose, drawn as a single bold solid shape with NO outline of any kind and no dark border, filled in pale bone-grey and warm stone-cream in two flat tones so the fill alone carries the silhouette against a very dark near-black interface panel, at most one simple division line between stones, no ground, no grass, no shadow, no landscape, no background scenery, no text, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the stack of stones on every side, an extremely simple graphic shape that stays clearly readable when scaled down to 16 pixels, strategy game interface icon


# ---------------------------------------------------------------------------------------------
# THE #249 SET — and the reason only three of the five candidates are written down.
#
# #249 migrates the HUD's remaining PICTOGRAPHIC emoji to sprites and leaves the tinted
# SYMBOLIC glyphs (⚠ ⚑ ⊘ ▦ ◎) as text. Applying that split to what is actually left in the
# client gives five candidates, and two of them cannot be generated independently:
#
#   forage  🌿  `FoodIcons.DEFAULT`, the work chip            -> SHIPPED (coverage 32%)
#   hunt    🦌  `FoodIcons.HUNT` + the 🏹 activity glyph      -> SHIPPED (coverage 6.8%)
#   scout   🧭  the roster activity glyph                     -> SHIPPED, see caveat
#   ---- NOT PROMPTED ----
#   tended  🌾  the plant ladder's rung-2 mark
#   penned  🐄  `DetailFormat.CORRAL_GLYPH`, the animal ladder's rung-3 mark
#
# THE LAST TWO ARE HALF OF A FOUR-MARK SET AND CANNOT MOVE ALONE. The ladder marks read
# as one column — plants `🌾 Tended Patch -> ▦ Field`, animals `◎ pastoral -> 🐄 penned` —
# and #249's own rule leaves ▦ and ◎ as text because they are symbolic. So migrating 🌾 and
# 🐄 would put two drawn sprites beside two text glyphs IN THE SAME COLUMN, at different
# weights and different vertical metrics, and the one job a rung mark has is to be told
# apart from the other three. Either the whole ladder migrates or none of it does, and that
# is a decision about the vocabulary rather than about the art.
# ---------------------------------------------------------------------------------------------


# forage.png — `FoodIcons.DEFAULT` (🌿), the work chip's foraging mark.
# STATUS: SHIPPED at `assets/icons/hud/forage.png` (coverage 32%, fill luma 196, 100%
# green-dominant so no hue collision with the staple's gold).
# ⚠ IT MUST NOT BE GRAIN. `crops/staple.png` is already a tall narrow gold wheat ear, and the
# crops section's own rule is that a `flora/` or role mark must never draw another slot's
# subject or a row says the same thing twice under two names. A foraging chip beside a staple
# row is exactly that collision, so this is a LEAFY SPRIG and its hue stays off the staple's
# gold (~45deg) — green ~110deg, which is also the `fodder` mark's hue, but fodder is LOW+WIDE
# and this is compact, and the two never share a surface.
forage: Flat vector interface mark of a small leafy sprig, a single short stem carrying three or four broad simple rounded leaves fanning outward, no grain, no wheat, no ears of corn, no seedheads, no flowers, no roots, no pot, drawn as a single bold solid shape with NO outline of any kind and no dark border, filled in a bright fresh sage green and a pale mint green in two flat tones so the fill alone carries the silhouette against a very dark near-black interface panel, at most one interior division line, no ground, no shadow, no background scenery, no text, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the sprig on every side, an extremely simple graphic shape that stays clearly readable when scaled down to 13 pixels, strategy game interface icon


# hunt.png — ONE mark replacing TWO emoji: `FoodIcons.HUNT` (🦌, the work chip) and the roster
# activity glyph (🏹). They mean the same thing and differ only in where they render, so the
# migration is the moment to collapse them.
# STATUS: SHIPPED at `assets/icons/hud/hunt.png` (coverage 6.8% — the SPARSEST mark here,
# and it reads only because the arrow is one unbroken bar. The rejected twin drew the
# bowstring in the same cream as the shaft, and at 13px the two merged into a plus sign
# with the arrow gone. Spend the cream on ONE thick element.)
# ⚠ IT MUST NOT BE AN ANIMAL, and the choice of the BOW is what makes that permanent. A deer
# here collides with `fauna/deer.png` on screens that show both: hunting is the ACT, the herd
# is the SUBJECT, and a mark that draws the subject cannot say which one a row means. Nothing
# thinner than ~1/6 of the icon width survives 13px, so the bowstring is drawn thick.
hunt: Flat vector interface mark of a simple hunting bow with a single nocked arrow, the bow a bold curved stave with a straight taut string and the arrow crossing it horizontally with a plain triangular head, no animal of any kind, no quiver, no hand, no target, no feather detail beyond one chunky flight, drawn as a single bold solid shape with NO outline of any kind and no dark border, filled in a warm amber-ochre and a pale bone tone in two flat tones so the fill alone carries the silhouette against a very dark near-black interface panel, thick sturdy strokes with nothing thinner than one sixth of the icon width, no ground, no shadow, no background scenery, no text, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the bow on every side, an extremely simple graphic shape that stays clearly readable when scaled down to 13 pixels, strategy game interface icon


# scout.png — the roster activity glyph (🧭).
# STATUS: SHIPPED on v2 at `assets/icons/hud/scout.png` (coverage 18.5%, fill luma 234).
# ⚠ V1 WAS TWO FOOTPRINTS AND IT FAILED AT SIZE — the lesson generalizes past this icon, which
# is why it is here. Two prints split the ink across FOUR separate pieces (two soles, two toe
# pads) inside a 13x13 grid, and both candidates came back as pale smudges at 7.7% and 10.0%
# coverage — the lowest in the set. ONE large footprint doubled the mass per shape and cleared
# it at 18.5%. The rule: at 13px, PIECE COUNT costs more than any single piece's size, so spend
# the icon's ink on the fewest shapes that still say the thing.
# ⚠ THE PROMPT CHANGES THE SUBJECT, WHICH IS A VOCABULARY DECISION, NOT AN ART ONE. The glyph
# in the client today is a COMPASS, which is several thousand years early for a roster that
# starts at a late forager tribe and whose top route rung is a paved road. Two tracks in the
# dirt says the same thing — someone went that way, ahead of the band — without the
# anachronism. Treat this as a candidate to look at rather than a settled replacement; if the
# compass stays, the prompt is wrong rather than the render.
scout: Flat vector interface mark of two bare human footprints in the dirt, one ahead of the other as if walking away, seen from directly above, each footprint a simple bold rounded sole shape with a single separated toe pad and no individual toes, no compass, no needle, no arrow, no map, no boot tread, drawn as a single bold solid shape with NO outline of any kind and no dark border, filled in a pale bone-cream and a warm mid tan in two flat tones so the fill alone carries the silhouette against a very dark near-black interface panel, no ground, no shadow, no background scenery, no text, centered with even padding on a plain solid magenta background, no rounded square, no tile, no card, no badge, no frame, no border, the flat magenta touching the footprints on every side, an extremely simple graphic shape that stays clearly readable when scaled down to 13 pixels, strategy game interface icon
