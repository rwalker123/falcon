---
paths:
  - "clients/godot_thin_client/src/scripts/Hud.gd"
  - "clients/godot_thin_client/src/scripts/ui/hud/**"
---

<!-- Extracted verbatim from lines 169-170;173-175;183-184;187-190;222-225 of clients/godot_thin_client/CLAUDE.md at blob 20553fb8f9b193b80338a8c06765d511b81b601e
     (the PRE-SPLIT original — read it with `git cat-file blob 20553fb8f9b193b80338a8c06765d511b81b601e`;
     clients/godot_thin_client/CLAUDE.md itself is now the hub, where the routing table lives).
     Regenerate with scripts/split_claude_md.sh -->

<!-- HAND-EDITED SINCE EXTRACTION: the "rules that keep it safe" + "the process" sections
     below were moved down out of the client hub (they only bind when you are inside
     ui/hud/, which is exactly what this file's paths: gate on). A re-run of
     split_claude_md.sh without re-pinning the blob would drop them. -->

# Hud.gd and the ui/hud module reference

The hub carries the module TAXONOMY — which kind a new piece of code is, and the rule that
`HudLayer` is none of them. This file carries what you need once you are actually extracting.

## The rules that keep it safe

Each was learned the hard way — see `docs/plan_hud_decomposition.md`.

- **Reflective / harness-reached methods stay as thin `HudLayer` delegators.** `Main.gd` reaches
  `HudLayer` by `has_method` / `has_signal`, and a failed probe **fails SILENTLY** (no error — the
  wiring simply never happens). So a controller emits its OWN signals and `HudLayer` RELAYS them, and a
  `has_method`-probed name is never moved off `HudLayer` (it keeps a thin delegator). The same applies
  to the `ui_preview` / `band_panel_preview` harnesses, which poke some `HudLayer` privates by DIRECT
  field access — those hard-error on a move (budget for the repoints).
- **Watch the hidden member straddle.** A bare `bool` / `int` / `Dictionary` written by one cluster and
  read by another is invisible to a call-graph scan and silently welds the two. **Before splitting a
  cluster, grep for shared MEMBERS, not just shared functions** — this bit the arc four times
  (`_grid_wrap_horizontal`, the three tint scalars, `_food_flow_present`, `_band_zone_tier`).
- **"An injection you still have to hold is relocated, not eliminated."** Moving a helper out while
  keeping the Callable to reach it has not reduced coupling. The real win is a controller holding a
  typed collaborator ref (`TargetingController` collapsed BandPanelController 6→3), or calling an
  all-`static` layer directly (FactionReadouts is now injection-free).
- **A `const` moves iff EVERY reader moved — but dependency DIRECTION outranks that rule.** A leaf
  (`HudStyle`, a vocab module) must NEVER be made to depend on `HudLayer`; a stray reader becomes a
  **downward alias** instead. And `const` initializers evaluate at class load, so a cross-class
  const-ref **cycle fails to load the WHOLE client** — keep vocab leaves acyclic (`HudConst` reads
  nothing) and honor the co-location constraints noted on the vocab-module row.
- **Extract shared layers BEFORE controllers.** A controller pulled out over a still-inline shared
  layer needs a dozen Callable injections to reach it; extracting the all-`static` layer first drops
  that surface dramatically (this took `DrawerComposeController` from 36 injections to 3).
- **A `RefCounted` can't `add_child` or `get_tree()`** — pass the HUD `CanvasLayer` as a host `Node`
  and parent / `await` through it (the `TurnOrbController` fork-panel pattern). **Reparenting a `%Name`
  node clears `unique_name_in_owner`** — pass scene nodes by reference, never reparent them.

**The process when a cluster genuinely needs extracting:** MEASURE first (grep the surface — functions
AND members AND every reflective/harness seam), verify each "X-only" claim's REACHABILITY (not just its
presence — a dead branch is not a dependency), then extract behaviour-neutral with `ui_preview` /
`band_panel_preview` / `marker_field_guard` as the safety net. The same discipline applies to the other
big client files that were decomposed the same way (the native `lib.rs` module map; `Inspector.gd` →
per-tab panels).

## Key scripts

| Script | Purpose |
|--------|---------|
| `Hud.gd` | HUD layer. The **legend card** (right-dock **TerrainLegendPanel**: `update_overlay_legend` rows `{color,label,value_text}` + the terrain-only **sort header** — `Name`/`Count` toggles with a ▲/▼ arrow, display-only field ∈ {name,count} × per-field direction, default **Count desc**, persisted across map regen) is composed out into `ui/hud/LegendController.gd`; Hud holds it as `_legend` and delegates `update_overlay_legend`/`toggle_legend`/`_on_legend_sort_pressed`. **The left-dock COMMAND FEED and its controller are GONE** (issue #272 — see `event-dock.md`): `ingest_command_events` now feeds the Telling alone, `reset_command_feed`/`toggle_command_feed`/`_note_command_feed` are deleted, and the client-side notes that used to land in the feed leave on a new **`system_note_requested(label, detail)`** signal (`note_system_event`), which `Main` relays to the event dock's System channel. MapView's `_build_terrain_legend` supplies a numeric `count` per row for the count sort; non-terrain (overlay/tag) legends hide the sort control. Also: the **selection card** — ONE card, ONE list, ONE drawer (`TilePanel`, `docs/plan_tile_panel_layout.md`): a pinned `%TileChips` strip, the selectable `%SubjectList` with the LAND as its first row, and the height-capped `%SubjectScroll`/`%SubjectBody` drawer whichever row is lit fills (land → `%TileDetail` + the `%ForageAssignControls` "assign foragers" stepper; herd → `%OccupantDetail` + the `%HerdAssignControls` "assign hunters" stepper+policy picker; expedition → `%AllocationPanel` Recall/Move). **Player-band detail relocated into the dockable `BandCityPanel`** (summary + `%AllocationPanel`-style labor UI render there via `BandPanelController.render_band`; the drawer renders a one-line pointer at it) — see "Band/City dockable panel". Turn readout (the standalone band Alerts panel was folded into the turn-orb attention model — see "Turn orb & attention model"). **The cross-cluster selection + labor state is extracted into two `RefCounted` models (Phase 0, `docs/plan_hud_decomposition.md`), held as `_selection` / `_band_labor`**: `HudSelectionState` owns the selection triplet (`tile_info()`/`unit()`/`herd()`), the lit-row kind `subject()`, the roster, and the sticky-selection guard `choice_tile()`; `HudBandLaborState` owns the snapshot-captured `player_band()` / `player_bands()` (the full player-faction list backing the band-picker + the panel cycler) / `player_expeditions()` / `panel_band()` / `world_herds()`, the grid scalars (width/height **and** the horizontal-wrap flag — `wrap_horizontal()`, which moved off `HudLayer` in Phase 2c-2b so every wrap-aware hex distance reads the pair from one place), the turn, the losing-population diff, the forage-patch / food-module lookups, and the optimistic `pending_labor()` overlay (incl. `effective_worker_map` / `effective_idle`). A THIRD model, `ComposeState` (Phase 2c-1), is held as `_compose` and owns the forage/hunt/party compose state + the open sheet's `kind`/`subject` (the sheet NODE stays on HudLayer). All three live in-file as `HudLayer` members; every former `_selected_*` / `_player_*` / `_pending_*` / `_forage_assign_*` / `_hunt_assign_*` / `_send_party_*` field is now an accessor/mutator call. (`_selected_food_module` / `_selected_food_is_hunt` were DELETED in 2c-1 — 7 writes, zero readers, since Phase 2a/2b took the readers and `SelectionCardController` re-derives `tile_info.get("food_module")` locally.) Roster selection emits `roster_occupant_selected`; labor edits emit `assign_labor_requested` / `move_band_requested` / `cancel_order_requested` (clear-all). **The shared forecast/estimate MATH has left this file** for `ui/hud/SourceForecast.gd` (all-`static`, stateless) — every yield readout, pre-commit forecast, worker cap and raid verdict is now a `SourceForecast.*` call, and the forecast vocabulary constants it owns are re-exported here as a labelled block of `const X = SourceForecast.X` aliases. **The shared WIDGET FACTORY and the FORMAT/vocabulary helpers have left too** — `ui/hud/HudWidgets.gd` and `ui/hud/HudFormat.gd` (both all-`static`, same idiom), so ~104 call sites here are now `HudWidgets.*` / `HudFormat.*`. What did NOT move to those static modules is the node-building that CANNOT be static — and **the drawer-only half of it has since left this file too**, into `ui/hud/DrawerComposeController.gd` (Phase 2c-2b): the signal-emitting factory (`_build_extend_pen_control` → `extend_pen_requested`, with its diffing twins), the member-reading ones (`_build_band_picker` reads `_band_labor`, `_build_compose_open_button` reads `_compose`) and the whole compose forecast/gate/picker layer (`_forage_policy_gates` / `_hunt_policy_gates` / `_tame_stalled_hint` / `_forecast_worker_cap` / `_forecast_yield_row` / the `_flora_entry_*` sub-layer / the two local preview lines …) all live there now. What is left on `HudLayer` is the one-line `_hex_distance_wrapped` pass-through (the band-panel band/work/parties zone BUILDERS left into `ui/hud/BandPanelController.gd` — Phase 2d, and the drawer RENDER DISPATCH left into `ui/hud/SubjectDrawerController.gd` — Phase 2c-3) (the three stylebox factories finally went to `HudStyle`, whose remit they always were, and the zone CHROME + the stacked composition bar to `HudWidgets` — see the `BandPanelController` shared-layer pass) that supplies the grid pair (from `_band_labor.grid_width()` / `wrap_horizontal()` now — the wrap flag moved onto the labor model). **The shared DETAIL-RENDER layer has left too** — `ui/hud/DetailFormat.gd` (all-`static`) plus its node-owning half `ui/hud/DisclosureController.gd` (held as `_disclosures`): the BBCode renderer, its key→tint registry, the ~20 detail label / `*_value_hex` leaves, the pure band food arithmetic, and the whole Food/Morale disclosure + popover cluster. **Three `HudLayer` members went with it and did not come back** — `_selected_band_food_turns` / `_selected_band_morale` / `_selected_band_output` were per-render OUT-PARAMETERS of the formatter, so they became fields on a `DetailFormat.Context` each host constructs LOCALLY and threads through its line producer; the four reset sites collapsed to two local constructions (`_render_occupant_drawer`, `_build_vitals_label`) and two deletions (`_render_selection_panel` / `_render_band_into_panel`, whose resets were only ever protecting a member that no longer exists). **The band/party DETAIL-LINE PRODUCERS have since left too**, in a three-way split: the ones that turned out PURE became `DetailFormat` statics once their one reach-out was threaded in as a parameter (`herd_summary_lines(herd, world_herds)` and the expedition tooltip trio `expedition_row_tooltip` / `_expedition_delivery_tooltip_line` / `expedition_next_delivery_line`, which take the already-resolved `target_herd`); the genuinely STATEFUL remainder went to `ui/hud/BandDetailLines.gd` (held as `_banddetail`); and `_format_stockpile_label` became `HudFormat.stockpile_label`, which also DELETED `FactionReadouts`' `_format_stockpile_label_fn` injection (both that helper and its two readers are since retired — see the `FactionReadouts` row). `_food_flow_present` went with them (it is a private handshake between two producers, both of which moved). What STAYS here of that family is `_herd_label_for_id` — the one genuine injection, since it reads `_selectioncard` AND `_selection` AND `_band_labor` (the LAND drawer's terrain-line producer `_tile_terrain_lines` left with the render dispatch into `SubjectDrawerController` — Phase 2c-3). `_meter_bar` moved to `HudFormat.meter_bar` (the pure format layer) and `_labor_assignments_of` consolidated onto `HudBandLaborState.labor_assignments_of` (a public static, merging the byte-identical copy that already lived there), so their readers call them as `HudFormat.X` / `HudBandLaborState.X` with no HudLayer method or Callable injection |
| `ui/hud/HudSelectionState.gd` | `RefCounted` state model (HUD decomposition Phase 0) — "what the player is looking at": the selection triplet (`tile_info`/`unit`/`herd`), the lit-row `subject` (`SUBJECT_LAND`/`UNIT`/`HERD`, aliased back onto `HudLayer`), the assembled roster, and the sticky-selection `choice_tile`. Encapsulated mutators (`select_tile`/`set_tile_info`/`select_unit`/`select_herd`/`select_land`/`set_subject`/`set_roster`/`note_choice_tile`/`clear`) each emit `changed(reason)` — emitted in Phase 0, consumed by nothing yet (Phase 2). Pure DATA, no node refs; accessors return by reference (matches the HUD's in-place-mutation) |
| `ui/hud/LegendController.gd` | Owns the right-dock legend card: row rendering, the terrain-only Name/Count sort header + its display-only sort state, the suppress toggle, and internal-scroll sizing. `HudLayer.LEGEND_SORT_FIELD_*` alias to its `SORT_FIELD_*` consts. Behaviour identical to the old inlined legend code |
| `ui/hud/FactionReadouts.gd` | `RefCounted` **MODEL** (HUD decomposition Phase 1a, `docs/plan_hud_decomposition.md`) owning the player faction's per-faction snapshot readouts — its sedentarization, its discovered Wondrous Sites and its intensification-ladder knowledge. **IT OWNS NO NODES — a pure MODEL, and it was `TopBarReadouts` until it stopped being one.** It rendered eight Labels in the HUD's top-right block (the Sedentarization meter, the `Pop …` demographics line, the discovered-sites strip, the `⚒ Your people know:` strip, plus `Turn N` and the `Units · Logistics · Sentiment` metrics line), and **issue #450 retired that whole block** — `TurnBlock`, `TopBar` and all eight Labels are deleted from `HudLayer.tscn` — because the Band/City dock's FACTION PAGE says the same things better: the PEOPLE bar for the demographics, the KNOWLEDGE zone for the other three, the turn orb's own face for `Turn N`. It was renamed in the same PR rather than left describing where the data used to be drawn. **What survives is the INGEST, and that is why it is still a cluster**: all three wire fields are per-faction ARRAYS, and this is the ONE place they are filtered to `PLAYER_FACTION_ID` and retained, so the faction page reads one answer through `faction_tracks` / **`faction_sedentarization`** / **`faction_discovered_sites`** rather than walking the arrays again and getting a second opinion about whose faction is being reported. **The DEMOGRAPHICS path is gone outright, ingest and all** — the PEOPLE bar sums the BANDS and apportions once, so a per-faction total would be a second source of truth for the head count; `Hud.update_demographics` and `Main`'s dispatch went with it and the wire section has no client reader. `render_overlay` went too: its turn readout is the orb's and its metrics line is retired, so `HudLayer.update_overlay` keeps only `_band_labor.set_turn` + `_turnorb.set_turn` (its `metrics` parameter is unread and stays, because `Main._hud_invoke` reaches the method BY NAME and a failed probe fails silently). The strip-only display constants went with the labels — `METER_BAR_CELLS` (the wider Sedentarization meter), `DISCOVERIES_GLYPH` / `DISCOVERIES_UNKNOWN_GLYPH` / `DISCOVERIES_STRIP_SEPARATION`, `KNOWLEDGE_STRIP_PREFIX` / `KNOWLEDGE_STRIP_TRACKS_PER_LINE` / `KNOWLEDGE_KNOWN_BADGE` / `INTENSIFICATION_SEGMENT_SEP` — as did `_rebuild_discoveries_strip` and its two node factories, `_knowledge_meter_text` and `_sedentarization_color`. `KNOWLEDGE_METER_CELLS` and `SEDENTARIZATION_STAGE_NONE` stayed: `FactionRollup` reads both, so the page and the ingest cannot disagree about what half-learned looks like or about the sim's own spelling of "no stage". **The left-dock Stockpiles card LEFT this cluster** (issue #381): it was retired for the band dock's Trade row, which is purely BAND-scoped (a per-turn rate, no faction stock), so **no HUD cluster reads `faction_inventory` any more** and `HudLayer.update_stockpiles` is gone with it — `MapPanel.apply_update` is the snapshot key's remaining consumer. Hud holds it as `_topbar` and keeps thin reflective delegators (`update_sedentarization`/`update_discoveries`/`update_intensification`, reached by `Main._hud_invoke`). Holds PURE data and no nodes; owns `_intensification_knowledge` / `_knowledge_announced` (moved off HudLayer; `_stockpile_totals` went with the retired card). Its `faction_knowledge()` is PUBLIC because HudLayer's policy-gate reasons (`_forage_policy_gates` / `_hunt_policy_gates`) read this cluster's knowledge back. **It takes exactly ONE Callable injection** — `_note_sink`, where the one-shot knowledge-unlock nudge goes. It was a `CommandFeedController` reference; with that feed retired it resolves to `HudLayer.note_system_event`, i.e. the event dock's System channel, and it is a Callable rather than a typed ref because the panel belongs to `Main` and neither this controller nor the HUD owns it. `TurnOrbController` (the unanswered-fork receipt) and `TargetingController` (the two quarry refusals) took the same swap for the same reason.** Otherwise every shared-but-PURE helper it used to inject or duplicate is all-`static` and called directly: **`HudFormat.meter_bar`** (the retired Sedentarization + knowledge strips, and `FactionRollup`'s two meters now; was the last injected Callable, `_meter_bar_fn` — dropped when `_meter_bar` moved to `HudFormat`, which the herd-drawer danger bars already wanted), `HudFormat.progress_percent` (was a second injected Callable), `HudWidgets.set_label_tooltip` (was a verbatim private COPY, deleted) (`HudFormat.stockpile_label` was the third injected Callable, `_format_stockpile_label_fn`; it is **deleted** — the Stockpiles card and the band drawer's accessible-stock rows were its only two readers and both are retired). Its ingest is behaviour-identical to the old inlined top-bar code; only the rendering is gone. |
| `ui/hud/DockScrollFit.gd` | Shared sizing for a dock card whose content grows without bound (**the selection card's subject drawer**, which calls the split-out `fit_height(scroll, content_height, …)` because its body is a VBox of live controls rather than a RichTextLabel — `fit(scroll, label, …)` is now written in terms of it, so the height math exists once — the Telling panel grows to fit its own bounded page capped at `PAGE_MAX_HEIGHT` now and no longer uses this): grow to fit the label, capped by the room left in the dock's `ScrollContainer` beneath it, so the card scrolls INTERNALLY rather than dragging the fixed panels through the dock scroll. **"Room left" excludes what the cards BELOW it need** (`_height_reserved_below`, summed over *visible* following siblings): a growing card used to always be the LAST in its dock so the distinction never arose, but the retired command feed sat ABOVE the fixed cards in its dock and claiming everything beneath it pushed them clean out of the visible dock. **The rule outlived the card that taught it** — the feed is gone (the event dock replaced it), so nothing in the tree currently exercises `_height_reserved_below`, and the next growing card placed anywhere but last would need it again. Only visible siblings count, so the both-hidden default is unchanged and toggling one on simply hands the room back. **Deliberately not `AutoSizingPanel`** — that one sizes a FREE-FLOATING control against the viewport (`global_position` + anchors + `offset_bottom`, as NarrativeForkPanel and the Inspector do), and a card inside the dock's VBoxContainer has neither: the container overwrites its size every layout pass and the ceiling that matters is the DOCK's remaining height. `PanelCard` + this helper is the container-side equivalent |
| `ui/hud/DockRowController.gd` | `RefCounted` controller owning ONE cluster: **where the bottom-bar chrome SITS relative to the Band/City panel's reserved row** (issue #324). On a **BOTTOM** dock it reparents `NavBacking` (minimap + zoom rail) into that panel's `RAIL_SLOT_TOP` and `TurnCluster` into `RAIL_SLOT_BOTTOM` — **ONE stacked column at the row's TRAILING end**, minimap on top — then takes `BottomBar` out of layout flow: `custom_minimum_size.y = 0` **AND** `visible = false`, because a zero-minimum but visible HBox still participates in `RootColumn`'s separation. **A leading + trailing gutter pair was built first and rejected on sight with a real minimap in it** — `NavBacking` is ~300px, so two opposite gutters cost ~562px of row, pushed the band zone inward AND stranded dead space around the orb; one column costs `max(nav, turn)` (**≈296–308 depending on map aspect** — 296 Standard, **308 Large**, since the rail width tracks the minimap's `grid_width / grid_height`; it is the NAV cluster either way, the turn cluster having shrunk to 116 when its `Turn N` caption moved into the orb face) plus one `BandCityPanel.RAIL_SEPARATOR_SPAN` gutter = **321 of row** on Standard, **333 on Large**, which hands the zones ~240px back and drops the wide→narrow flip well below the ~1618px of window width the two-gutter shape needed. **The ABSOLUTE figures live in `band-city-panel.md`, not here** — the work zone is what the flank widths leave after the rail, so a flank retune moves them and two rule files stating them is how they drift (they did: this row carried the pre-widening 923px and ~1377px flip for a while after `ZONE_BAND_WIDTH`/`ZONE_PARTY_WIDTH` moved). What this row owns is the RAIL's own width, which is the term that differs by map aspect — and 308 on Large is why a flank width that clears the board's 2-column floor on Standard can fail it there. Hud holds it as `_dockrow`, constructed in `_ready` **AFTER `_connect_zoom_rail()`** (that call applies the nav backing's stylebox, hence its padding, hence the minimum size this controller measures), and keeps ONE thin reflective delegator, **`reflow_dock_row(edge, size)`** — `Main._connect_band_city_panel` connects it as a SECOND listener on the panel's `reservation_changed` and seeds it once, so a session that boots already docked bottom reflows immediately. **THE FIT GATE IS ONE TEST:** `_required_height()` = both clusters' minimum heights + `BandCityPanel.RAIL_SLOT_SEPARATION` + the card's own vertical chrome (`CARD_CHROME_V`, read off the panel's public consts so the two cannot drift), and the reflow happens iff a panel is injected, the edge is `SIDE_BOTTOM` (`REFLOW_EDGES` — **`SIDE_TOP` was removed in issue #377**: `set_reserved_inset` only displaces `BottomBar` when the inset and the bar share an edge, so a top dock had nothing to recover and relocating dragged the minimap and orb to the top of the screen), and the reserved size is at least that. That single comparison covers *every* case that must fall back to the stacked layout — the panel collapsed to its 46px rail, the panel hidden (0), and a window short enough that `MAX_WIDE_HEIGHT_FRACTION` clamps the strip under the chrome — so none of them needs its own branch. The chrome term is IN it deliberately: the gate is compared against the RESERVED cross-axis size, which includes the card's border + content margins, so omitting it would pass a strip whose interior cannot hold the stack — and a short window is exactly the case being tested. Measured at 1080p: the stack needs **322 of a 360px strip**, so it fits with room, while a collapsed 46 correctly declines. **WHY THE WHOLE BAR AND NOT JUST THE MINIMAP:** `NavBacking` ≈ **164** tall (minimap `embedded_height` 140 + `MinimapContainer` margins 4+4 + `HudStyle.NAV_BACKING_PADDING` 8+8, measured exactly), `TurnCluster` `custom_minimum_size` = **(116, 128)** — it was (260, 128) until the `Turn N` caption moved into the orb face, and only the WIDTH changed, so the fit gate is unmoved — `BottomBar` `custom_minimum_size.y` = **150** — so relocating the minimap alone recovers only ~14px, and the turn cluster plus that authored 150 hold the floor. `BottomBar`'s minimum is **captured from the scene at construction, never hardcoded**. **OWNERSHIP IS THE OPPOSITE OF `set_zones`:** the HUD owns the chrome NODES and does all the reparenting; the panel owns only the empty rail SLOTS — do not "unify" the two. **`_move_into` writes NO anchors, offsets or presets**, and that is the point: a slot is a `MarginContainer`, so it lays the cluster out and the panel's `ALIGNMENT_CENTER` stack centres the pair in the strip. `PRESET_FULL_RECT` would stretch the cluster; the tempting `PRESET_HCENTER_WIDE` silently does NOT centre a plain `Control` (it centres a `Container` and leaves a plain one pinned to the mid-line growing downward — measured, see `BandCityPanel._build_rail` note 3). It DOES overwrite the clusters' size flags (`SHRINK_CENTER`, to centre a cluster narrower than the column), so `_home` captures each cluster's parent + child INDEX + anchors/offsets/size-flags before any reflow and restores them verbatim. Neither reparented subtree carries a `%Name` (verified against `HudLayer.tscn`) — reparenting one would silently clear `unique_name_in_owner`. While reflowed it re-declares the rail width off `minimum_size_changed`, because the minimap's width tracks the map's grid aspect (`MinimapPanel.resize_to_aspect`); the stack's HEIGHT needs no push, since the slot containers read their own child's minimum. Verified by `band_panel_preview`'s five `band_panel_dockrow_*` states |
| `ui/hud/HudWidgets.gd` | **All-`static`, stateless** shared WIDGET FACTORY (HUD decomposition, the `DrawerComposeController` precursor) — every reusable Control the HUD builds out of raw Godot nodes + this project's chrome vocabulary, lifted verbatim out of `Hud.gd`. The stepper family (`build_worker_stepper` / `build_two_line_stepper` / `build_row_name_label` / `build_row_note_label` / `build_status_part` / **`add_stepper_controls`** — the most shared primitive of the set, 5 sites across 4 clusters — and `build_party_stepper_row`) · the zone chrome (`zone_head` / `build_section_menu` / **`make_zone_column`** / **`make_zone_block`** / **`wrap_zone`** / **`clear_children`** — the containers all three Band-panel zones AND the flat no-dock fallback host are assembled from) · the stacked **`build_composition_bar`** + **`build_composition_key`** (one proportional-bar primitive, two questions: PEOPLE's muted who-they-are and WORKFORCE's saturated what-they-do) · the take-policy radio (`build_policy_picker` + the private `_policy_rung_cell` / `_policy_rung_line`, which build a rung as a `MarginContainer` CELL — an empty-`text` Button under a two-Label stack, since two font sizes cannot live in one `Button.text` — and the `POLICY_RUNG_META` the harnesses identify a rung by) · the improvement control (`build_improvement_control` + `IMPROVEMENT_STATE_*` + `IMPROVEMENT_CONTROL_META`), the second axis's own widget — offered/gated/running/done. **`gate_reasons` is gone**: #442 moved gating off the stance picker entirely, so `RungGates.gate_reasons_for` is the survivor and it lives with the gates, not with the widgets) · the harvest-floor family (`build_floor_picker` / `build_floor_chart` / `build_crew_targets` + its `_crew_target_pill` / `build_verdict_line`) and the compose sheet's READOUT (`build_readout_box` / `build_yields_row` / `build_readout_aside` / `build_dashed_rule` — the last one drawn rather than styleboxed, and drawn with the THIN-LINE primitive for a reason `labor-ui.md` records) · the label leaves (`alloc_section_label` / `alloc_hint_label` / `build_inline_link` / `forecast_label`) · the compose sheet's FIELD-ROW pair **`build_field_key`** + **`build_option_picker`** — the shared key label (one DECLARED width, `HudComposeVocab.COMPOSE_FIELD_KEY_WIDTH`, so `Band:`, `Kit` and `Quarry` line their value controls up across three modules) and the native `OptionButton` two of them mount. **`build_picker_menu` is GONE with the kit picker's conversion** — it was a `MenuButton` whose face carried a `⌄` GLYPH, which `clip_text` ate on a long kit name and which could never match the themed arrow the `Band:` picker drew one row above; `build_option_picker` draws that arrow as a reserved-margin ICON and marks the current entry natively, so it takes no `MENU_ENTRY_CHECKED`. `_fill_menu_popup` survives with `build_section_menu` as its one caller, which is the `⋯` menus' path and the quarry chooser's. The rule the pair implements is in `labor-ui.md` → "The compose sheet's FIELD ROWS are one family" · **`build_marker_icon`** — the species/site MARK on a text row, bundled art where the client has it and the `FoodIcons` emoji where it does not (issue #439). It exists because **Unicode ships ONE deer**: `HERD_SPECIES` maps `deer`/`elk`/`reindeer`/`caribou`/`gazelle` all to 🦌, so the work board, the work inspector, the quarry picker and the selection card’s roster could not tell four distinct roster species apart long after `FaunaSprites` had split the MAP marker. One builder for THREE of the four surfaces — the work row, the inspector strip and the roster rows — so those cannot drift from each other or from the map. **The compose sheet's quarry picker is deliberately NOT one of them**: its host is a `Button`, which carries art on its own `icon` property (`_build_quarry_row` sets `icon` + `expand_icon` + the `icon_max_width` theme constant), so routing it through a builder that returns a `Control` would mean parenting a child into a Button face for no gain. It resolves `FaunaSprites` only — a quarry is always a herd — which is the other reason it is not a caller. **Do not "unify" it back in**: the mechanism is chosen by HOST widget, and a Button is a different host (`sprites-widgets.md`). **Return a `Control`, not a `TextureRect`** — the `null` branch is a glyph `Label` and stays load-bearing at full art coverage (an unknown species, `HERD_DEFAULT`, and the land row’s module-less `◈`, which is not a site at all). **A `TextureRect` rather than `[img]` BBCode, and the host widget is what decides that**: `CropRoleSprites` uses `[img]` because its host genuinely IS a `RichTextLabel`, while every host here is a `Label` in an `HBoxContainer` — the precedent is `StageSprites` + `BandCityPanel.set_header`, which swaps a `TextureRect` in for a glyph `Label`. Do NOT convert a row to `RichTextLabel` to get art into it. **The sprite is drawn UNTINTED and nothing may set `modulate` on what it returns**, the map markers’ own rule: a full-colour animal carries no state, so state rides GEOMETRY beside it (the work row’s severity stripe, the roster row’s ecology dot, the marks column). **THE GLYPH BRANCH IS THE OPPOSITE CASE, and the trailing `glyph_color` is how a host says so.** Splitting the mark out of its host's label is what dropped the colour it used to inherit from that label's text (`"%s %s" % [glyph, name]`), and a bare `Label` plus this client's absent `Theme` renders at Godot's STOCK near-white — so a `◈` on an `INK_DIM` roster row read brighter than its own name and stopped dimming with it. `null` (the default) means "set no override", which is what every stock-coloured host — the work row's mark, the work inspector's — had before the parameter existed and still wants; a host whose text is STATE-TINTED passes the same colour it gives that text. **The untinted rule and this one do not conflict**: art carries no state and a glyph is text, so the branches take opposite treatment by design. Callers size it from their own vocab module — `HudWorkVocab.WORK_ROW_ICON_WIDTH`/`WORK_ROW_FONT_SIZE` on the board and its inspector, `HudSelectionVocab.ROSTER_ROW_ICON_BOX`/`ROSTER_ROW_ICON_FONT_SIZE` on the roster; the quarry picker is the one host that takes art as a **Button `icon` property** instead, capped by the stock `icon_max_width` theme constant (`HudComposeVocab.COMPOSE_QUARRY_ICON_MAX_WIDTH`) so a 256px source cannot set the button’s minimum and drag the compose row wide · and the three MUTATORS, which fix up a Control someone else made and are NAMED for it: **`compact`** (was `_compact_control` — it squeezes a Control's type + stylebox chrome in place, it is not a factory), **`trim_button_padding`** (the CHROME half of `compact` — a control that must keep its TYPE SIZE and trim only the box; it leaves a button's SIDE padding as `HudStyle` authored it, a zone row being short on height and not on width) and `set_label_tooltip` (a `Label` defaults to `MOUSE_FILTER_IGNORE`, so a bare `tooltip_text` is a SILENT no-op — route every Label tooltip through here). **WHY ITS OWN FILE:** these are called from FOUR clusters being split apart (drawer compose, WORK zone, PARTIES zone, selection card), so lifting the drawer out with them left behind meant injecting ~12 `Callable`s into the new controller — and the Band-panel extraction would then need the same 12 again. Same measurement that produced `SourceForecast`. **STATELESS IS THE INVARIANT** — the ONE piece of HUD state any of it needs is threaded as a PARAMETER: `build_worker_stepper`/`build_two_line_stepper`'s trailing **`current_turn`**, used solely to seed `ArrivalStrip.set_schedule` (defaulted to `ArrivalStrip.UNKNOWN_TURN`, since only the two-line + gappy-schedule path reads it). Consts stay on `HudLayer` and are read back as `HudLayer.X` (the `SelectionCardController`/`FactionReadouts` pattern); it calls `HudFormat.*` freely, never the reverse. **What deliberately did NOT move HERE:** a factory that EMITS a signal (`_build_extend_pen_control`), the member-reading ones (`_build_band_picker` / `_build_compose_open_button`) and the stylebox factories (`HudStyle`'s remit). The first three were not static-able, but they were drawer-local — they travelled to `DrawerComposeController` in Phase 2c-2b instead, where the signal is the controller's own; the styleboxes lingered on `HudLayer` until the `BandPanelController` shared-layer pass finally put them where the note always said they belonged (`HudStyle.role_card_stylebox` / `work_row_stylebox` / `work_inspector_stylebox`) |
| `ui/hud/HudFormat.gd` | **All-`static`, stateless** shared FORMAT / VOCABULARY layer (HUD decomposition) — the pure `String`/`int` helpers deciding how the HUD SAYS a thing, with no Control in sight: the status chain `status_label` → `status_tooltip_line` → `append_status_tooltip` → `join_tooltip_lines` (plus `expedition_phase_label`, which `status_label` defers to for a PHASE so the phase words are typed once), the row glyph affixes (`source_icon_prefix` / `row_glyph_suffix`), the rung `policy_face` (glyph + name — the ONE policy vocabulary the picker, the gate-reason lines, the work inspector's standing-investment line and its confirm all share), **`plant_crew_label`** (the plant web's crew noun keyed on the source's standing rung — `Foragers` on wild ground, `Tenders` on a Tended Patch or a Field; it lives here rather than in `SourceForecast` because that layer references no `Hud*Vocab` module by invariant, and the rule it implements is in `labor-ui.md` → "The plant web's crew noun follows the STANDING RUNG"), the two vocabulary absorptions from the drawer extraction (`food_module_label` + its `FOOD_MODULE_LABELS` table, and `band_display_name`), the **expedition ROW vocabulary** from the `BandPanelController` shared-layer pass (`expedition_phase_key` → `expedition_phase_suffix` → `panel_expedition_summary`, the last taking `herd_label_for_id` as a **Callable PARAMETER** rather than reaching for the roster/selection/herd-list state a stateless layer must not hold), the **largest-remainder `apportion_people`** (the reason the PEOPLE brackets sum to the band's own size instead of rounding to 31 people in a band of 30) with `dependency_per_hundred` / `dependency_tooltip`, `progress_percent` (0..1 → whole percent, **clamped** — the one meaningful difference from the two open-coded `int(round(x * PROGRESS_PERCENT_SCALE))` sites left in the extend-pen pair), and **`meter_bar(score, cells)`** (the block-glyph `▰▱` bar; landed here from `HudLayer._meter_bar` since THREE clusters read it — the Sedentarization + knowledge strips via `FactionReadouts`, the herd-drawer danger bars via `DetailFormat` — and it touches no member, so it drops `FactionReadouts`' last Callable injection). **WHY SEPARATE FROM `HudWidgets`:** `FactionReadouts` needs `progress_percent` and nothing else, so the words live where a cluster can depend on them without importing a builder of Controls it never builds. The word TABLES (`STATUS_LABELS` / `STATUS_HINTS` / `EXPEDITION_PHASE_LABELS`) stay on `HudLayer`, read back as `HudLayer.X` |
| `ui/hud/DetailFormat.gd` | **All-`static`, stateless** shared DETAIL-RENDER layer (HUD decomposition, the `BandPanelController` precursor) — everything that turns a list of `"Key: value"` detail LINES into the BBCode the HUD's detail surfaces show. Three families: **the renderer** `detail_bbcode` (+ the private `_split_kv` and the per-row key→tint registry `_value_hex`, which every detail surface in the game consults — the tile card's Sight/Habitability/Ecology cases sit beside the band's Food/Morale/Growth ones) · **the ~20 label / `*_value_hex` leaves** those tints and the line producers share (`ecology_phase_label` / `ecology_value_hex`, `append_danger_component_lines` + its `_danger_*` privates, `graze_range_label`, `husbandry_*`, `herders_*`, `cultivation_*`, `field_*`, `flora_composition_text`, `corral_*`, `pen_feed_*`, `sight_is_live` / `sight_value_hex`, `expedition_mission_label`, `morale_cause_label`, `food_turns_text`, `morale_is_concerning`) · and **the pure band-dict food arithmetic** (`band_net_food` / `band_food_income` / `band_pen_feed` / `band_has_food_flow` / `sum_realized_yield` / `food_is_concerning` / `breakdown_key` / `food_breakdown_row` / **`band_provisions`** / **`merged_arrival_schedule`**), which lives here because the Food summary row, its breakdown AND the Band panel's FOOD OUTLOOK chart all read it — the last two are that chart's own two inputs (the larder it starts from, and every source's `arrival_schedule` summed slot-by-slot), pure band-dict arithmetic like the rest. **THE KEY DESIGN MOVE — the smuggled tint context is gone.** `detail_bbcode` used to read four `HudLayer` members (`_selected_band_food_turns` / `_selected_band_morale` / `_selected_band_output` / `_disclosure_state`) that were pure per-render OUT-PARAMETERS: written by the line producers, reset by four different hosts, read only here. They are now ONE explicit value parameter, the `DetailFormat.Context` RefCounted (`food_turns` / `morale` / `fertility` / `disclosures` — the `output` scalar has since gone with the row it tinted, productivity now reading on the Band panel's WORK zone head, which is `Label`s rather than BBCode and needs no context; `fertility` arrived with the Growth row), constructed LOCALLY by whichever host is about to render, filled by the producer it passes it to, and handed straight to the renderer — so a stale context is structurally impossible and the reset ORDER stopped mattering. The three scalars left `HudLayer` entirely. The other impurity, the Attack/Defense bars' roster reference, is likewise **threaded in** (`world_herds`), the treatment `SourceForecast` gave the grid-wrap pair. **STATELESS IS THE INVARIANT** — no node, no `_hud`, no snapshot cache. The two shared helpers it calls are class-name statics for exactly this reason (`HudFormat.meter_bar` for the danger bars, `HudBandLaborState.labor_assignments_of` for the Gathered/Hunted sums); everything else it needs is a param. **It also owns the PURE line producers** — the ones that held no HUD state once their single reach-out was threaded in as a parameter: **`herd_summary_lines(herd, world_herds, assigned_herders)`** (the whole herd drawer — size/biomass/range/overgrazing/ecology/danger/husbandry/herders/corral/pen/position; its impurities are the `world_herds` roster the Attack/Defense bars normalize against and `assigned_herders`, the ACTUAL staffed-herder count threaded in for the Herders row — see the Herd-staffing bullet) and the **expedition tooltip trio** `expedition_row_tooltip(exp, phase, target_herd)` → `_expedition_delivery_tooltip_line` → **`expedition_next_delivery_line(exp, target_herd)`** (the "Next delivery: …" wording the parties strip, the drawer and the row tooltip all share; the party's migrating target is resolved by the caller through `HudBandLaborState.expedition_target_herd`). The STATEFUL half of that family is `BandDetailLines` — see its row. **CONSTS follow one rule: a const lives here iff EVERY reader moved here** — so the `DANGER_*` family, the `HERDERS_*` labels + `HERDERS_ROW` + `FULLY_HERDED` + `HERDERS_SHED_FORMAT`, the build-verb labels + `CORRAL_PROGRESS_COMPLETE`, `PEN_FEED_ROW` + the two pen-starving formats + the `PEN_FOOTPRINT_*` / `PEN_FEED_SPLIT_*` rows, `HERD_SIZE_ROW` / `HERD_RANGE_ROW` / `HUSBANDRY_*_HINT` / `OVERGRAZE_EPSILON` / `OVERGRAZING_WARNING`, the `EXPEDITION_NEXT_DELIVERY_*` pair + `EXPEDITION_RECURRING_GLYPH` + `EXPEDITION_ROW_FOCUS_HINT`, the **recovery-guidance pair** (now spelled structurally, `RECOVERY_GUIDANCE_TEXT := RECOVERY_GUIDANCE_GLYPH + …`, so the "the text begins with the glyph" invariant the tint registry depends on cannot drift), the ▸/▾ carets and the larder-runway pair (`FOOD_RUNWAY_UNIT` / `FOOD_UNLIMITED_GLYPH`) came; every const still shared with `HudLayer` code (the `Food`/`Morale` row keys, `MORALE_BREAKDOWN_INDENT` + the sign glyphs, `MORALE_CAUSE_*`, `MORALE_TREND_EPSILON`, `STORE_ITEM_PROVISIONS`, `FOOD_FLOW_MIN`, `CORRAL_GLYPH`, the `FOOD_LABEL_*` table, `BREAKDOWN_TOGGLE_META_PREFIX`) stayed and is read back as `HudLayer.X`, the `HudWidgets`/`HudFormat` convention. It deliberately does NOT own the popover those carets open — see the next row |
| `ui/hud/DisclosureController.gd` | `RefCounted` controller (HUD decomposition) owning the **detail-row disclosure cluster** — the NODE half of the detail-render layer: the per-render caret state (`state()` → `DetailFormat.Context.disclosures`), the stashed breakdown payloads, the shared breakdown `PopupPanel`, and the `meta_clicked` dispatch. **The cut with `DetailFormat` is along NODE OWNERSHIP, not "formatter vs popover"** — the two are bidirectionally coupled (the formatter emits the `[url]` meta only this file parses; the popover renders THROUGH the formatter), so what separates them is that this half must `add_child` a Window, which a static module cannot and a `RefCounted` cannot either. Hence `setup(popover_host: Node, refresh_hosts: Callable)`: the HUD CanvasLayer is the host (the `TurnOrbController` pattern), and `refresh_hosts` collapses the old `BandPanelController.render_band` + `_render_subject_drawer` pair into **ONE injected Callable** — the single inbound re-render edge, since WHICH hosts can show a caret is `HudLayer`'s knowledge. Surface: `setup` · `wire_label(label)` (replaces BOTH `meta_clicked` bind sites — the drawer's long-lived `%OccupantDetail` and the Band panel's per-render vitals label, each binding ITSELF as the popover's anchor) · `state()` · `clear_rows()` · `register(row_label, kind, band, lines)` · `food_breakdown_lines(band)` · **`kit_breakdown_lines(band)`** (the band's three consumable kits, each beside the role IT sets — see `band-readouts.md` → "The band's KIT"; the pairing is the readout, and rendering one carry tier on the other kit's row is the defect that split exists to prevent). Hud holds it as `_disclosures`, constructed in `_ready` after `_drawercompose`. **The breakdown opens in a POPOVER, never inline, and that is a correctness rule** — inline growth of the vitals label happened AFTER the Band panel had picked its zone height tier, and the `clip_contents` zone hosts then sliced the WORKFORCE row and ate both role cards |
| `ui/hud/hud_const.gd` · `hud_disclosure_vocab.gd` · `hud_attention_vocab.gd` · `hud_selection_vocab.gd` · `hud_expedition_vocab.gd` · `hud_work_vocab.gd` · `hud_compose_vocab.gd` · `hud_flora_vocab.gd` · `hud_event_vocab.gd` | **Topic VOCABULARY modules** — `class_name`d, ALL-`const` / zero-func / zero-var leaves that hold the HUD's word tables, glyphs, formats and thresholds. The old `HudLayer` const block was decomposed into these to stop it being a merge-conflict surface (every feature adding a label/glyph touched `HudLayer`). Behaviour-neutral: values unchanged, only their home + every reader's reference. **The old `read back as HudLayer.X` idiom is gone** — a moved const now reads `HudConst.PLAYER_FACTION_ID`, `HudWorkVocab.WORK_ROW_HEIGHT`, `HudComposeVocab.LOCAL_HUNT_POLICY_HINTS`, `HudSelectionVocab.ROSTER_DOT_SIZE`, `HudExpeditionVocab.EXPEDITION_PHASE_LABELS`, `HudAttentionVocab.ATTENTION_KIND_STARVING`, `HudDisclosureVocab.BREAKDOWN_TOGGLE_META_PREFIX`, `HudFloraVocab.GATE_REASON_*` (or `DetailFormat.CORRAL_GLYPH` / `FOOD_LABEL_*` / `MORALE_*`, absorbed there). **`HudConst` reads nothing** (cycle-safe leaf); three co-location constraints keep the split acyclic — `FOOD_LABEL_*` ship with `CORRAL_GLYPH` in `DetailFormat`; `STATUS_LABELS`/`STATUS_HINTS` ship with `EXPEDITION_PHASE_*` in `HudExpeditionVocab`; `HudFloraVocab.FLORA_CROP_ROW_FONT_SIZE` is a DOWNWARD alias of `HudWorkVocab.WORK_ROW_FONT_SIZE`. Config/build/legend/travel consts and the `LEGEND_SORT_FIELD_*` aliases STAY on `HudLayer` (read only by its own `_ready`/config code). **`hud_event_vocab.gd` (`HudEventVocab`) is the newest of them** and the one whose reader is NOT the HUD: it holds the event dock's importance model (rungs, channels, glyphs, the detail floor) and is read by `ui/EventDockPanel.gd`, a CanvasLayer `Main` owns. It lives here anyway because it is a vocabulary leaf and this is where vocabulary leaves live — see `event-dock.md`. |

---

