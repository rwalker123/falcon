[package]
name = "integration_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
core_sim = { path = "../core_sim" }
sim_proto = { path = "../sim_proto" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
